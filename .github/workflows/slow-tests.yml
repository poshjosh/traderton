name: Slow Tests

on:
  pull_request:
  push:
    branches: [main]

jobs:
  parity-drift:
    name: parity-drift
    runs-on: ubuntu-latest
    env:
      PARITY_DRIFT_CI: '1'
      HEROBIDS_ROOT: ${{ github.workspace }}/herobids
      TRADERTON_ROOT: ${{ github.workspace }}/traderton
    steps:
      - uses: actions/checkout@v4
        with:
          repository: poshjosh/herobids
          ref: 250dd60b37603105b2028b93972ec54fa5c06d33
          path: herobids
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          path: traderton
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: corepack enable
      - working-directory: traderton
        run: pnpm test:slow