# Anomalies & Deviations

**Status:** living
**Purpose:** log things discovered **during** copy-and-delete that force an
unplanned deviation from pure copy — genuinely broken source behaviour, or a
seam that cannot be preserved without authoring non-trivial logic.

## When to add an entry

Add an entry when, mid-extraction, you hit any of:

1. source behaviour that appears genuinely broken;
2. a seam that cannot be stubbed narrowly and would require authoring
   non-trivial logic to keep the build/tests green;
3. a copied test that cannot pass without a real (non-stub) change.

**Do not improvise a fix.** Stop, log it here, and surface it. A silent
authored fix is the exact failure mode this project exists to avoid.

## Not for this doc

- **Deliberate up-front design decisions** → [000-vision.md](./000-vision.md)
  decisions list + [001-parity-ledger.md](./001-parity-ledger.md) (Intentional
  divergence). Example: bots being mechanical-only is a planned decision, not an
  anomaly.

## Log

| Date | Where (file / seam) | What | Decision / status |
|------|---------------------|------|-------------------|
| 2026-09-05 | **herobids SOURCE-CHANGE REQUEST #1** — the strategy registry: `packages/domain/src/config/strategy-parameters.ts` (+ `LlmParamsSchema`/`HybridParamsSchema` and the `initStrategyRegistry` call in `config/schema.ts` ~2159–2165) | **What the registry is (so ownership is clear):** a validation lookup table keyed by `(strategyType, decisionMode)` → `{ schema, defaults, description }`. Its real job is `validateStrategyParams()` — parse a strategy's params against the schema for its (type, mode) — behind the trading-owned `StrategyIdentitySchema`. It uses deferred init (`initStrategyRegistry({ mechanical, hybrid, llm, empty })`) purely to break a module cycle with `schema.ts`. **Ownership conclusion: the registry is TRADING-owned and moves to Traderton** — its whole vocabulary is trading strategy types/modes and it is the enforcement point for bot strategy config (decision 2, own config). What does NOT belong here are two of its *entries*: `momentum:hybrid` (`HybridParamsSchema`) and `momentum:llm` (`LlmParamsSchema`) — the `llm`/`hybrid` decision modes are the agent's *reasoning* modes relocated by decisions 7–9 (bots are mechanical-only). So this is the same seam as "bots are mechanical-only," seen from config-validation. **Requested behaviour-preserving herobids change:** extract the `llm`/`hybrid` decision-mode registration and the `LlmParamsSchema`/`HybridParamsSchema` to the agent side, leaving a **mechanical-only strategy registry** (registers `mechanical` + `dca`; `SUPPORTED_DECISION_MODES` narrows accordingly; `initStrategyRegistry` no longer requires `llm`/`hybrid`). herobids stays behaviour-equivalent for its own agent paths (the llm/hybrid registration continues to exist agent-side); Traderton copies a registry that is mechanical-only. Related deliberate divergence to decide/record at source: `StrategySchema.decisionMode` enum `['mechanical','llm','hybrid']` → Traderton is mechanical-only, so narrowing this enum is a behavioural change that must be a logged **Intentional Divergence** (decisions 7–9), not a silent edit. | **RESOLVED (2026-09-05).** herobids implemented the refactor (commit `648f9110`, "refactor(domain): make strategy registry mechanical-first"; released at HEAD `0ef05b8f`), behaviour-preserving (herobids's 195 config tests green). It took plan Option B: `initStrategyRegistry({ mechanical, empty })` no longer requires llm/hybrid, and a new `registerAgentDecisionModes({ llm, hybrid })` hook registers the agent modes; `LlmParamsSchema`/`HybridParamsSchema` definitions remain in herobids `config/schema.ts` (moving them would break its re-export surface). **Traderton then:** re-synced `strategy-parameters.ts` verbatim; kept `initStrategyRegistry({ mechanical, empty })` but does NOT call `registerAgentDecisionModes` (Intentional Divergence — mechanical-only); deleted `LlmParamsSchema`/`HybridParamsSchema` + types as clean leaf deletions. Traderton's registry is mechanical-only (`mechanical`+`dca`); `momentum:llm`/`momentum:hybrid` resolve as unsupported (guarded by an adapted `strategy-parameters.test.ts`). Build + copied domain tests green. |
| 2026-09-05 | ~~**herobids SOURCE-CHANGE REQUEST #2** — `blueprint.ts` agent/bot discriminated union~~ **WITHDRAWN** | **Withdrawn after investigation — no herobids change needed.** The original concern was that `blueprint.ts` fuses agent (platform) and bot payloads in one discriminated union. Investigation of herobids consumers showed **`blueprint.ts` is the platform marketplace/authoring layer, not part of the trading path.** Evidence: (1) bot creation/validation/execution use **`BotConfigSchema`** (in `config/schema.ts`, trading-owned, clean) — `apps/api/routes/bots.ts`, `apps/worker/agents/agent-message-broker.ts`, `apps/worker/index.ts`; (2) the bot execution path (`agent-trading-actor.ts`, `packages/engine`) has **zero** blueprint references; (3) `BotBlueprintRevisionPayload` is used only by the marketplace API (`apps/api/routes/blueprints.ts`, `services/blueprint-projection.ts`, `blueprint-risk-resolver.ts`), and `blueprint-projection.ts` *projects a bot row into* a blueprint payload — i.e. the blueprint is a marketplace **view over** `BotConfigSchema`, which is the source of truth. **Resolution:** `blueprint.ts` is platform → **delete it from Traderton** (clean leaf deletion; Intentional Divergence "blueprint marketplace/authoring stays platform" — see [001](./001-parity-ledger.md)). Do NOT keep a hollowed `blueprint.ts` containing only `BotConfigSchema`: that file matches no source file and would break the file-for-file 1:1 diff discipline (`BotConfigSchema` lives in `config/schema.ts`, not `blueprint.ts`). 1:1 parity is preserved by *deleting* the platform file (diff = "file removed"), not by repurposing it. Bot reproduction/cloning as a Traderton-native feature is recorded as a planned future capability in [004](./004-decision-log.md) + [001](./001-parity-ledger.md), built on `BotConfigSchema`; not built in Phase 1. |
| 2026-09-05 | `packages/domain/src/config/schema.ts` (+ `config/index.ts`, `agent-protocol.ts`) | **The config schema is a fused monolith.** `config/schema.ts` is 2598 lines / 212 exported symbols — roughly 90 clearly-platform (billing, auth, plans, LLM runtime, alerts, telegram, gmail, browser-pool, services, platform-assessment) and ~54 trading-core (Risk*, Bot*, TokenSafety*, Strategy*, Execution*, Technical*, indicator params, venue/permission consts), interleaved line-by-line. Some single symbols fuse both concerns (e.g. `AGENT_STYLE_RUNTIME_DEFAULTS` mixes LLM token budgets with trading tick/risk defaults; `WakePreferencesSchema` pulls `AgentWakeSourceSchema` from the platform `agent-protocol.ts`, which in turn imports the deleted platform `tool-schemas.ts`). Reducing this file to trading-only is done by **leaf-first in-place deletion of platform blocks** (line-level deletions, diff-visible against source), keeping the file 1:1 with the source in identity/structure. Most platform schemas delete cleanly this way. The two edges that a deletion CANNOT cut without authoring are pulled out as their own entries above (source-change requests #1 and #2). | **RESOLVED (2026-09-05).** `config/schema.ts` reduced to trading-only entirely by clean leaf-first in-place deletions — **no authored restructuring**. Sequence: (1) `WakePreferences`/`AgentWakeSourceSchema` seam (deleted platform wake schema + `agent-protocol.ts` + `config/load-providers.ts` + `config/assessment-config.ts`); (2) after `blueprint.ts` was deleted (platform, separate entry) the agent-policy schemas lost their last consumer; (3) after herobids source-request #1 the LLM schemas became leaf-deletable. Final: deleted `AppConfigSchema` + platform type block, the agent-runtime-policy island, LLM block, billing/plans/stripe/creem/usageBilling, auth, alerts/telegram/gmail, nomad/sharedServices, worker/webAccess/services/browserPool/httpClient/sessionCircuit/agentRuntime(+Policy), marketIntelligence, platformAssessment*, evaluation, apiConfig/modelDefaults, agentCost/agentRisk/agentApprovals, Intelligence/UnifiedAgent/preset-policy/capability/hybrid/authorization schemas, WakeGateConfig, validateReviewInterval, and the agent `AgentStyle`/`PermissionLevel`/`DEFAULT_PERMISSION_LEVEL`. Result: 2568→844 lines, 77 exports, all trading. Barrel + tests pruned to match. Build + lint + 230 copied tests green. The file remains 1:1 with herobids `config/schema.ts` in identity/structure (diff = platform blocks removed). |
| 2026-09-06 | `packages/venues/package.json` — `ccxt` dependency pin (Phase 5) | **ccxt version drift breaks the verbatim copy's build.** herobids `packages/venues/package.json` declares `ccxt: ^4.4.0`, but its lockfile resolves and green-builds against **`ccxt@4.5.54`**. The Phase 5 plan copied the `^4.4.0` spec verbatim; under Traderton's fresh install that range resolved to **`ccxt@4.5.77`**, whose `bybit` class shape no longer sufficiently overlaps the local `BybitExchangeWithAccountMode` cast in `bybit.ts:408` (TS2352) — a compile error in a copied, byte-identical trading file. Verified: (a) the copied `bybit.ts` is byte-identical to source (diff = namespace rename only); (b) herobids' own `packages/venues` `tsc --build` is green at 4.5.54; (c) the only difference is the resolved ccxt version. This is a build-environment/dependency-resolution drift, **not** a trading-behaviour change and **not** an authored logic edit. | **RESOLVED (2026-09-06) — dependency pin, Intentional Divergence from the plan's `^4.4.0` spec.** Pinned `ccxt` to the exact version herobids' lockfile resolves (`"ccxt": "4.5.54"`) so the copied source compiles identically to the tested source. Reproducing the source's exact resolved dependency is scaffolding to preserve behaviour (same class as retargeting operator config / mirroring lockfile state), not authoring trading logic; the copy-never-author law is intact. After the pin: `@traderton/venues` builds clean (only the expected browser-pool seam remained, then cut), lint + full test suite green. |
| 2026-09-06 | **Phase 8 STOP-GATE** — `apps/worker` boundary/ownership decisions (see [docs/features/008-worker-plan.md](./features/008-worker-plan.md)) | The Phase 8 formal keep/delete classification (read-only) found the mechanical trading loop *can* be module-separated from the agent runtime (every platform package dep is a clean deletion), BUT four coupled decisions trip 009 stop-gates 1–4 and cannot be resolved from the code/docs alone: **(1) drive-path/005 boundary shape** — the decision core is driven today by the platform `AgentMessageBroker` over a Redis stream; Traderton must be driven by the 005 HTTPS boundary (`submit_decision`), which is authored boundary infra, not copied logic (is the HTTP handler Phase 8 or Phase 9?); **(2) `resolveBotStartupContext`** reads `bot.connectionId`/`connections`/`bot.userId` (all dropped by decisions 11–13) — can't compile against `@traderton/db`, can't be cut by deletion; needs a herobids source-fix OR an authored venue-account-direct soft-seam, and dropping the connection-status/mismatch guards is a consequential behavioural divergence needing sign-off; **(3) the `create_bot`/`start_bot` limit KEY** (Phase-2 Deferred-required) — agent/agents-row-locked today; Traderton has no `agents` table, so the new key (per-owner / per-venue-account / operator config) is a tenancy-shape decision; **(4) trading-tool ownership** Phase 8 vs Phase 9 (009 assigns the 25 tool endpoints to Phase 9). | **RESOLVED (2026-09-06) — human-approved, all four decisions.** **(1) Drive-path/boundary:** NO API/REST layer in Phase 8. Traderton (pre-authoring) is packaged as a **library** whose in-process intake core (`actorRegistry` + `agent-intake-resolver` + `submitDecisionForExecution` + the two actors + `WorkerRuntime`) herobids consumes to **replace its existing in-process trading** — herobids stays the consumer, calling the core in-process as it does today. The 005 REST/API layer is layered above the intake core **later** (Phase 9 / the final authoring pass), not now. **(2) `resolveBotStartupContext`:** the connection→venueAccount front-end is a platform grant file → **deleted from Traderton** as Intentional Divergence (NOT a source-request — the seam cuts cleanly by deletion: the file terminates in a `venueAccountId` string and the whole venue-account-onward trading path already speaks `venueAccountId`, so nothing is authored). Guards split on the ownership line: the **connection-grant guards** (`connection_not_usable`, `connection_venue_account_mismatch`, `missing_connection_id`, `connection_not_found`) stay herobids (validate a herobids-owned concept); the **venue-account existence guards** (`missing_source_venue_account`, `source_venue_account_not_found`) move to Traderton (validate what Traderton owns). Traderton's startup/actor context takes an injected `venueAccountId`. **(3) `create_bot`/`start_bot` limit:** NOT a source-request (no behaviour-preserving reshape exists — herobids' limit is definitionally `agents.maxBots`/`plan.entitlements`-keyed, both platform; the atomic `BotRepository` limit methods were already deleted Phase 2). NOT authored now (honors the defer-authoring direction). Phase 8 copies the mechanical create/start path with **no native limit**; the capability stays **`Deferred (required for cutover)`** on the `create_bot`/`start_bot` ledger rows and the cutover gate; **per-`ownerId` enforcement is authored in the final authoring pass** alongside the API/tenancy model. Cutover is blocked until it lands, so Traderton can never ship weaker than herobids-today. **(4) Trading-tool ownership:** Phase 8 keeps the trading LOOP + actor/intake core; Phase 9 (`apps/api`) owns the 25 trading-tool modules + boundary handlers (per 009). See [docs/features/008-worker-plan.md](./features/008-worker-plan.md) for the finalized plan. |
| 2026-09-06 | **Phase 8 classification correction** — the agent decision-intake/approval/session cluster in `apps/worker` (`agents/agent-intake-resolver.ts`, `agents/agent-decision-handler.ts`, `agents/actor-state-owner.ts`, `services/approval-service.ts`, `agents/instance-event-publisher.ts`) | The finalized Phase 8 checklist listed these 5 files as KEEP (mechanical intake core). At implement time, reading them in FULL (not just top-level imports) showed they are **platform**, not the mechanical loop: `agent-intake-resolver.resolveActiveBinding()` is built on the dropped `connections`/`agentConnections` grant model (a "venue-account-direct" version would be AUTHORED, not copied); the cluster needs symbols MISSING from the Traderton barrels (`agentConnections`, `AgentRepository`, `MessageEnvelope`, `DecisionSubmitPayload`, `getProviderIdsForRuntimeFamily` — verified absent) and carries Telegram approval / session-ownership / user-pubsub / authorization-mode platform behaviour. Consumer check across all of herobids: the cluster is imported ONLY by platform composition roots (`index.ts`, `agents/index.ts`, `agent-message-broker`, `agent-session-manager`, `market-intelligence/*`, `reminder-coordinator`) — **zero mechanical trading-loop files import it.** Root cause of the mis-classification: the coordinator's edge-analysis script checked only top-level relative imports and did not validate symbols against the barrels, so it missed the cluster's `import type` + runtime-value edges into DELETE files. | **RESOLVED (2026-09-06) — reclassified DELETE (Intentional Divergence); within decision-4 delegation, no new stop-gate.** The 5-file cluster is not copied into Traderton (platform decision-intake/approval/session surface). Its **capabilities** — `submit_decision` intake wiring, agent grant→venue-account resolution, human approve/reject — become **`Deferred (required for cutover)`**, owned by **Phase 9** (`apps/api`), where the boundary + the venue-account-direct resolver are authored deliberately (decision 4: "Phase 9 = the tool modules + boundary"; M1/M2 model). This is a clean deletion → no source-fix warranted. The reduced **39-file mechanical KEEP set is import-closed** (verified; only non-KEEP edge is the sanctioned `./runtime-composition` scan-types seam), so Phase 8 proceeds as the pure mechanical trading loop. Checklist + plan updated; ledger records the deferred capabilities against the relevant tool/subsystem rows. |
| 2026-09-06 | **Phase 8 STOP-GATE (crux) — Phase-1 domain slice is missing trading types the trading loop needs** | Implementing the reduced 39-file mechanical worker loop surfaced that **8 `@traderton/domain` symbols the loop imports were removed in Phase 1** and are absent from the domain barrel: `AppConfig`, `AppConfigSchema`, `TradingSessionName`, `ReasoningLevel` (from `config/schema.ts`, which Phase 1 trimmed to trading-only), and `WatchPurpose`, `WatchPurposeEnum`, `AgentWakePayload`, `ContextSnapshotPayload` (from `agent-protocol.ts`, which Phase 1 **deleted entirely as platform**). (Correction 2026-09-07: `AgentRiskDefaultsConfig` is ALSO absent from the `@traderton/domain` barrel — it is defined in herobids as `type AgentRiskDefaultsConfig = AppConfig['agentRiskDefaults']`, a projection of `AppConfig`, so it belongs to the deferred config surface; `agent-risk-limits.ts` was quarantined for exactly this. It appears in Traderton only inside comments in `agent-risk-contract.ts`.) They split into THREE kinds, and the split is the crux: **(a) trading types wrongly dropped** — `WatchPurpose`/`WatchPurposeEnum` (values `entry|exit|stop_loss|take_profit|monitor|alert` — a *trading* watch-purpose taxonomy, used by the KEEP trading files `watch-types.ts` + `position-coverage.ts`) and `TradingSessionName` (trading session windows, used by `tick-gates.ts`); these are trading-owned but were collateral in the `agent-protocol.ts` delete / `config/schema.ts` trim. **(b) genuinely-agent types the trading loop still references** — `AgentWakePayload`, `ContextSnapshotPayload` (agent wake/context envelopes, used by `agent-trading-actor.ts` + `complete-technical-scan.ts` via the optional agent-callback path), `ReasoningLevel` (scout/judge reasoning, used by `tick-thinking.ts`). **(c) the config monolith** — `AppConfig`/`AppConfigSchema` (used by `config.ts` the worker config loader + `public-stream-routing.ts`); Phase 1's ledger explicitly removed `AppConfigSchema` when trimming `config/schema.ts` to trading-only. **Compounding finding:** `index.ts` cannot be a faithful trading subset — the trading actors (`TradingActor`, `AgentTradingActor`) are *constructed by* the deleted startup/session/intake wiring, so there is no separable trading composition root without authoring. | **RESOLVED (2026-09-06) — human-approved, all four directions.** Re-analysed each symbol's actual usage before deciding. **(1) Trading types wrongly dropped → herobids SOURCE-FIX REQUEST #2** (detailed work-order in `.ignore/source-fix-request/002-relocate-trading-types-out-of-agent-protocol.md`, next row): relocate `WatchPurpose`/`WatchPurposeEnum`, `TradingSessionName`, **and** the two trading-adjacent payload types `AgentWakePayload`/`ContextSnapshotPayload` out of platform `agent-protocol.ts`/`config/schema.ts` into a trading-owned domain module (behaviour-preserving), then Traderton re-syncs `@traderton/domain` to copy them — correcting the Phase-1 over-deletion. This keeps the KEEP files (`watch-types.ts`, `position-coverage.ts`, `tick-gates.ts`, `agent-trading-actor.ts`, `complete-technical-scan.ts`) **verbatim copies** (option (a): re-home the payload types rather than edit the copied files' shape). **(2) `ReasoningLevel` → resolved by DELETE, not re-home:** its only worker consumer is `tick-thinking.ts`, which resolves agent **scout/judge** reasoning levels — verified agent (only `agent.ts`, a DELETE file, consumes `tick-thinking.ts`; no mechanical KEEP file imports it). So `tick-thinking.ts` is reclassified **DELETE** and `ReasoningLevel` drops off the KEEP dependency set entirely. **(3) `AppConfig`/`AppConfigSchema` (`config.ts` loader + `public-stream-routing.ts`):** Traderton owns its config (decision 2) → a Traderton-owned config shape is required; this is **authoring, `Deferred (required for cutover)`** (authored at M1-integration/Phase 9), so `config.ts` is NOT a verbatim copy. **(4) `index.ts` composition:** confirmed a faithful trading subset is impossible (actors constructed by deleted startup/session/intake wiring) → Phase 8 delivers the loop **MODULES + copied parity tests (green)** as the M1 library surface; the trading composition root is **`Deferred (required for cutover)`**, authored at M1-integration/Phase 9. **Phase 8 is therefore BLOCKED on source-fix request #2** (the loop cannot compile green until the 5 relocated types land in `@traderton/domain`); the deferred config + composition authoring is separately queued for M1-integration/Phase 9. Partial uncommitted work in `packages/worker/` (scaffold + copied mechanical files + `scan-types.ts` + placeholder `index.ts`) is retained pending the re-sync. |
| 2026-09-06 | **herobids SOURCE-FIX REQUEST #2** — relocate trading-owned types out of platform `agent-protocol.ts` / `config/schema.ts` (Phase 8). Detailed work-order: `.ignore/source-fix-request/002-relocate-trading-types-out-of-agent-protocol.md` (not version-controlled, per convention). | Phase 1 deleted `agent-protocol.ts` (platform) and trimmed `config/schema.ts` to trading-only, which collaterally dropped **five trading / trading-adjacent types the mechanical worker loop imports**: `WatchPurpose` + `WatchPurposeEnum` (watch taxonomy — trading; used by `watch-types.ts`, `position-coverage.ts`), `TradingSessionName` (session windows — trading; used by `tick-gates.ts`), and `AgentWakePayload` + `ContextSnapshotPayload` (trading market-event / reconnect payload shapes; used by `agent-trading-actor.ts`, `complete-technical-scan.ts`). Traderton cannot author them (copy-never-author + 1:1 diffability). **Requested behaviour-preserving change:** relocate these (plus the wake-context schemas the `AgentWakePayload` discriminated union depends on) into a trading-owned domain module, re-export from the barrel, update the platform files to import them — herobids stays behaviour-equivalent (its existing domain + worker tests are the acceptance bar). `ReasoningLevel` and `AppConfig` are explicitly NOT part of this request (former → `tick-thinking.ts` is DELETE; latter → deferred Traderton-owned config authoring). | **RESOLVED (2026-09-07).** herobids implemented the relocation behaviour-preserving (commits `6187449a` "refactor(domain): relocate trading-owned types out of agent-protocol" + `5a8d511e` "refactor(domain): rename watch-protocol to trading-protocol"; released at herobids HEAD `dcdeec36`; herobids domain build clean + **981 domain tests green, zero test edits** — verified by the coordinator, not self-report). New module `packages/domain/src/trading/trading-protocol.ts` holds all five requested symbols + the five wake-context schemas (with their `type` exports) + `AgentWakePayloadBaseSchema`/`WakePriority`; old files re-export to preserve the public surface. **Traderton re-sync (2026-09-07):** copied `trading/trading-protocol.ts` **verbatim** (byte-identical to source; its only imports are `zod` + `../market-assessment.js`, both present in Traderton) and wired it into the domain barrel with an explicit `export * from './trading/trading-protocol.js'` — Traderton uses an explicit barrel line rather than herobids' indirect re-export chain because Phase 1 deleted the platform re-export hosts (`agent-protocol.ts`) here (a sanctioned consequence of the Phase-1 divergence, not authoring; the copied module is 1:1 with source). Domain build + lint clean; **230 Traderton domain tests still green**. The five types (`WatchPurpose`, `WatchPurposeEnum`, `TradingSessionName`, `AgentWakePayload`, `ContextSnapshotPayload`) + relocated wake-context schemas → **Met** in the domain slice. Phase 8 unblocked. Precedent: request #1 (strategy registry, RESOLVED). |
| 2026-09-07 | **Phase 9a STOP-GATE — the tool contract (`ToolContext`/`AgentTool`/`ToolResult`) is platform-fused and absent from `@traderton/domain`** | 9a's copy of the 25 trading tool modules is blocked at the foundation: every trading tool (`account`, `bots`, `trading`, `schema`, `market-data`, `watch`, …) imports `AgentTool`/`ToolResult`/`ToolContext` from `@herobids/domain` (some also `getToolSchema`/`listToolSchemaNames`, `AGENT_MESSAGE_TYPES`). All of these are **absent from the Traderton domain barrel** — Phase 1 deleted `tools.ts`, `tool-schemas.ts`, and the `agent-protocol` message types as platform. Verified. Root cause (repeat of the Phase-8 lesson): the 9a "clean copies" classification checked package-level imports (`@herobids/domain`) but NOT whether the specific symbols survived the barrel. **The crux:** `ToolContext` (herobids `packages/domain/src/tools.ts:106`) is genuinely FUSED — trading fields (`executionMode`, `authorizationMode`, `botRepo`, `marketDataRegistry`, `redis`) interleaved with platform/agent fields (`agentId`, `sessionId`, `phase:'scout'|'judge'`, `permissionLevel: PermissionLevel` [deleted platform type], skills/browser/capability/usage-billing hooks). So the tool contract is not a clean trading module a deletion can separate — same class as the Phase-1 `config/schema.ts` monolith and `blueprint.ts`. Every trading tool depends on it → the ENTIRE 9a tool surface is blocked until the contract is resolved. (API routes are separately blocked on the authored Fastify app-shell + deleted platform config — consistent with 9b.) | **OPEN — STOP-GATE, awaiting human direction.** Not resolvable by 9a copy-only, not by authoring-past. Candidate directions (human decision): **(1) herobids SOURCE-FIX REQUEST #3** — split a trading-only tool contract out of the platform-fused `tools.ts` (a trading `ToolContext` without `permissionLevel`/scout-judge-phase/skills/browser/capability/usage-billing; `AgentTool`/`ToolResult`/`ToolDefinition`/`ToolCategory` as needed), behaviour-preserving, then Traderton re-syncs `@traderton/domain` and 9a copies the tools clean. Precedent: requests #1 (strategy registry) + #2 (agent-protocol trading types). **(2) Reclassify the tool contract + tool surface as 9b AUTHORING** — bundle the trading tool contract with the composition-root authoring (the tools become part of the authored M1 surface, not a copy). **(3) Hybrid** — source-fix the pure-data contract types (`AgentTool`/`ToolResult`/`ToolDefinition`/`ToolCategory` + `tool-schemas`), author only the fused `ToolContext` shape in 9b. Also implication: the 9a/9b split needs adjusting — the tool surface is NOT purely copyable; only whatever tools compile once the contract lands are 9a, the rest 9b. No code/docs authored past this gate; 9a marked Blocked. |
