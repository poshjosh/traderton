# 011 — Pre-merge backlog (autonomous driver-loop execution order)

**Status:** living. **Created:** 2026-09-12. The sequenced, cutover-BLOCKING work the
continuous driver loop (008 §8) executes top-to-bottom. Grounded by a code-verified inventory
(Contemplator, 2026-09-12) — no stale "done-but-marked-open" among blocking items. Optional /
non-blocking items are in §2 (NOT gate obligations). Update as slices land.

Legend: [ ] open · [~] in progress · [x] done (→ ledger row). "4-risk" = must route via 008 §3.

## Wave A — Traderton-side boundary tools (prereqs; mostly copy/adapt)
- [x] **A1. `delete_bot` boundary tool + herobids DELETE re-point.** DONE (traderton `9954470`; herobids `f5677ff9`). Owner-scoped `delete_bot` tool authored as a thin route→tool seam (mirrors `deprovision_venue_account`: `ownerScopedNoVenue` + `write-database`, owner-scoped existence via `getBotByIdForOwner`→`not_found.resource`, running-guard→`bot.running` mapped to 409 at the route, hard delete via `deleteBotByIdForOwner` — no FK points to `bots` so nothing orphans). herobids `bots.ts` DELETE now routes over the boundary AUTHORITATIVELY (fail-closed 503 when absent; `bot.running`→409 backstop; boundary-first, then the INTERIM local `db.delete(bots)` mirror-sync that D1 removes with the table). 4-risk (authoritative-removal vs local-only contract) settled by parity. **D1 sub-obligation still open:** the 409-backstop functional test once the pre-guard/local mirror is removed.
- [x] **A2. Owner/bot-scoped read wave: costs / journal / journal-summary / sessions / events.** DONE (traderton `a25c839`; herobids `b5818310`). Owner-scoped read tools `get_owner_bot_costs` / `get_owner_bot_sessions` / `get_owner_bot_journal` / `get_owner_bot_journal_summary` (+ repo methods, re-keyed to `ownerId`) added Traderton-side; aggregation stays Traderton-side, identical response shapes. herobids `/bots/:id/{costs,sessions,events,journal,journal/summary}` all re-point over `readBoundary(...)` with a local-table fallback (ruling 5 — the interim `fills`/`journalEvents` reads slated for removal at D1). ownerId semantics = ratified default C.

## Wave B — agent-container market-data read-tools (long pole; gates registry removal)
- [x] **B1. Hybrid sizing + `get_price`/`resolvePriceTarget` contract.** DONE (traderton `resolve_price_target` tool + herobids boundary adapter; settled by parity; HIGH parity-break fixed in review). Provides the `resolve_price_target` shared surface for B3.
- [x] **B2. `tools/price.ts` → boundary `get_price`.** DONE (herobids agent `get_price` tool → boundary get_price; boundary-first branch mirroring risk-limits.ts, in-process fallback preserved; settled by parity — byte-identical boundary tool). Lint clean; 18/18 price tool tests pass. No CRITICAL/HIGH in review.
- [x] **B3. Watch tools → boundary (Option C landed; B3-monitor Deferred as own slice).** herobids `tools/watch.ts`. Corrected scope (`resolve_watch` is NOT here — it lives in `resolvers.ts`, routes with the resolvers group later). Decision agent ruled Option D (settled within rules, no ratification): Traderton `check_watches` = evaluation authority; the herobids platform monitor becomes a thin boundary consumer. Must land ATOMICALLY:
  - [ ] **B3-pre** (traderton `bin.ts`): wire `new InstrumentRepository(db)` into the boundary context factory. Unblocks `watch_token` protective auto-link parity + boundary `find_instrument`. Mechanical/parity-preserving.
  - [x] **B3-tools** (herobids `tools/watch.ts`): DONE (herobids `353f584e`). `list_watches` read path + in-process fallback; `watch_token`/`remove_watch`/`check_watches` fail-CLOSED writes (mirror `adjust_risk_limits`), in-process write bodies DELETED (net −1719 LOC; removes trading-state authority from the agent process). Lint clean; watch tests green.
  - [x] **B3-summary** (herobids `agent.ts`): DONE (herobids `353f584e`). New pure `agent-watch-view.ts` helpers + `loadActiveWatches(agentId)` → single `list_watches` boundary fetch per tick derives both raw list + summary; empty→stable-digest + local fallback preserved. 8 helper unit tests.
  - [x] **B3-monitor-disable** (herobids `index.ts`): DONE (herobids `353f584e`). `watchThresholds` family forced `false` — the local watch push loop no longer evaluates the now-remote set (no split-brain). Watch-threshold WAKES are OFF until B3-monitor below. Discovery/regime families untouched.
- [x] **B3-monitor (DONE — watch-threshold wakes re-enabled).** Traderton `check_watches` now returns `reset[]` (edge-down watchIds, traderton `576638d`); the herobids platform monitor's `evaluateWatches` re-pointed to source triggered/reset from boundary `check_watches` per active+subscribed agent (via an injected `evaluateAgentWatches` port built in index.ts from the system sideEffectBoundary + `agents.userId` ownerId lookup + per-agent subject), clearing its wake-dedupe on reset (parity with main); removed local scan/getLatestPrices/edge-trigger/hset/refreshSummaryCache; `watchThresholds` family re-enabled (config-driven). No trading watch STATE touched in the herobids process. Worker tsc clean; market-intelligence suite 306/0 (1 skip). No CRITICAL/HIGH in review (1 MEDIUM fixed: reset-del now logs on failure for observability parity). — herobids pending commit.
- [ ] ~~**B3-monitor (Deferred-required; own slice).**~~ (superseded — DONE above) Re-point the platform watch-evaluation PUSH loop per Option D: monitor gains a `db` dep + `agentId→ownerId` lookup + a per-agent write-boundary factory; `evaluateWatches` invokes boundary `check_watches` per active+subscribed agent and consumes `triggered[]`, keeping platform work local (session/prefs/scanner-gated filters, dedupe, rate-limit, `emitMarketWatchTriggered`/`enqueueWake`). Re-enables watch-threshold wakes. Restructures the monitor test suite. Within the existing boundary contract (no new surface). RESTORES the temporary gap opened by B3-monitor-disable.
- [x] **B4. `tools/market-data.ts` → boundary.** DONE (settled by parity; herobids re-point of all 5 read tools — search_tokens/discover_tokens/check_regime/get_funding_rates/get_market_overview — over `ctx.tradertonBoundary`, in-process fetch retained as transitional fallback; discovery enrichment + search policy happen Traderton-side, no double-processing). Lint clean; 14/14 tests. No CRITICAL/HIGH in review.
- [x] **B5. Volatility-candle series behind the boundary.** DONE (Option B, 004 decision-log; routed + settled within rules, no ratification). New Traderton `get_volatility` boundary read tool (fetches BTC 1h/limit-24 candles Traderton-side, returns derived `{ volatilityPct }` — NEVER raw candles); `calculateAtrPercent` copied verbatim into `@traderton/market-data/indicators.ts`. herobids re-point (Approach 1): tick-gate dep `fetchVolatilityCandles`→`fetchVolatilityPct`, `resolveAdaptiveIntervalMs({volatilityPct})`, capability gate now `(registry OR boundary)` mirroring regime; boundary non-success→throw→degrade; in-process fallback preserved. Both repos type-clean (direct worker tsc); traderton 77/77, herobids 113/113. No CRITICAL/HIGH in review.
- [x] **B6. Economic-calendar acquisition behind the boundary.** DONE (Option A, 004 decision-log; routed + settled within rules, no ratification — the earlier "Deferred-required/shape TBD" was superseded, all prereqs present). Traderton: `get_economic_calendar` boundary read tool (cacheOnly, returns `{events,sources,fetchedAt}`; degrades `economic_calendar_not_configured`); `economicCalendarProvider` on `TradingToolContext`; port `cacheOnly?` alignment; acquisition loop (Scrapfly fetch + fallback parser + Redis cache warm + setInterval) RELOCATED into `boundary/src/bin.ts`. herobids: per-tick read re-pointed over the boundary (null-tolerant degrade preserved); in-process provider + acquisition loop + EC imports DELETED (grep-confirmed zero EC references remain in herobids/apps/worker/src); pure `parseEconomicCalendarBoundaryPayload` helper in venue-intelligence.ts + tests. Both repos type-clean (direct package tsc); traderton tools 16/16, herobids venue-intelligence 30/30 + worker suite 3081/0. No CRITICAL/HIGH in review.
- [x] **B7. Remove in-process market-data from the agent container (Wave B capstone).** DONE (Option D, 004 decision-log; routed + settled within rules; ONE pending-ratification item = the fail-fast-guard behaviour change). The Traderton read boundary is now the SOLE market-data source in the herobids agent process. Removed: `createProviderRegistry`/`createPriceService`/`marketDataRegistry`/`priceService`/`marketDataConfig` constructs + every in-process fallback branch (venue-intel perps+DEX, regime, volatility, hybrid-sizing); the 6 read tools (get_price + 5 market-data) are boundary-only + fail-closed `market_data_not_configured`; capability gate → boundary-only; MARKET_DATA_TOOLS visibility re-keyed to `!tradertonReadBoundary`. Added a FAIL-FAST startup guard (trading skills + no read boundary → throw; non-trading agents unaffected). ToolContext TYPE fields kept (Traderton uses them) — just unpopulated. Advances D1 (agent.ts no longer value-imports @herobids/market-data registry). Grep-confirmed isolation endpoint; worker tsc clean; full worker suite 3078 total / 0 failed / 11 pending. No CRITICAL/HIGH in review.

## Wave C — swap-path safety + swap scoring
- [x] **C1. Swap-venue token-safety gating.** DONE (settled by parity, 004 C1 decision; closes ledger Item B). `enrichTokenWithDiscovery` copied VERBATIM from herobids `main` index.ts:93-152 into `packages/worker/src/swap-token-enrichment.ts` (+ 8-case unit test); `swapTokenSafety` wired into `create-trading-runtime.ts` per-bot ActorFactory via the already-copied `createSwapTokenSafetyAdapter` + the herobids `resolveTokenData` closure (CanonicalResolver + DexScreenerProvider + resolveSwapTokenData + conditional enrich), guarded `config.marketData && sharedMarketDataRegistry` (orderbook/paper keep it undefined); override repo built once-per-runtime; 1inch `swapNetwork` fail-closed guard re-added. Composition seam tests added (adapter built for swap+marketData, not for orderbook/no-marketData). worker tsc clean; worker suite 1105/0. No CRITICAL/HIGH in review.
- [x] **C2. Swap `score_candidate` build (token→pool, T2).** DONE — verification found the build ALREADY LANDED (the "BUILD DEFERRED / inert" note was STALE, superseded by the market-intelligence group's swap-arm work). `packages/worker/src/tools/strategy.ts` `score_candidate` fully supports the swap arm: `network+tokenAddress` → `scannerPoolResolver` (pool resolution behind the boundary) → `selectCanonicalPool` (highest liquidity, tie-break volume then poolAddress, NO quote-asset constraint — the ratified decision) → `scannerCandleFetcher` (GeckoTerminal candles) → `scoreCandidate`; also the Option-A `poolAddress` path. Both open build items resolved: quote-asset default = none (ratified, in-code); GeckoTerminal free tier works (proven by `boundary-e2e.ts` swap check on free endpoints). Wired in `bin.ts` (scannerCandleFetcher + scannerPoolResolver). 15/15 strategy tests pass incl. 7 swap-path cases. Approach was human-ratified (Option B). No new work needed.

## Wave D — teardown, data migration, proofs (LAST)
- **D1. Delete herobids in-tree trading packages + tables + remaining value imports.** SPLIT after the 2026-09-12 classification (004 "D1 — remaining-value-import classification", decision agent). D1 is NOT one slice: only the mechanical removals (GROUP 2 + table drops + trading-provisioner) are safe now; GROUP 1 (candle-evidence) + GROUP 3b (trading key-gen) each need a Traderton-side re-point FIRST.
  - [ ] **D1-a (mechanical, autonomous NOW).** Remove GROUP 2 dead public-stream imports (`PublicStreamPool`/`BybitPublicStream`/`HyperliquidPublicStream` — `publicStreamPool` is constructed-then-only-shutdown; `createScopedStreamPoolHandle` has zero non-test callers; drop the dead test as Intentional-divergence). This is the ONLY fully-unblocked mechanical D1 removal.
  - **D1-c — trading-table-drop WAVE (its own multi-slice re-point programme, NOT a mechanical step; 004 "D1-tables — trading-table-drop scoping").** GROUNDED AUDIT (2026-09-12): the local `bots/fills/journalEvents/venue_accounts` tables STILL have live KEPT-PLATFORM in-process readers; a local trading-state table read in-process is the same laundering the GROUP 1 ruling rejected, and §3.1 unconditionally deletes the trading DB. So each reader re-points over the boundary FIRST (A2-shaped copy/adapt), then the tables drop. A/B/D settled within rules → **autonomous NOW**; only the `user_credentials` split (C) is human-blocked. Sequence:
    - [x] **D1-c1 — evaluation-evidence reads (#1). DONE.** Authored 3 agent-scoped boundary read tools (`get_agent_fills`/`get_agent_journal_events`/`get_agent_positions`; botIds folded server-side) + re-pointed `agent-evaluation/evidence-assembler.ts` (per-agent SYSTEM subject) AND `apps/api/routes/exports.ts` agent endpoints (per-request USER subject). Sessions stays local (platform table). Documented in 005. See 004 "D1-c1".
    - [x] **D1-c2 — assessment-identity read (#2). DONE.** Authored `get_agent_venue_binding` boundary tool (traderton `bots.ts`; resolves agent bot → venue account server-side, returns `{venueFamily,venueType}|null`) + re-pointed `market-intelligence/assessment-identity-resolver.ts` `resolveAgentBinding` via injected `resolveVenueBinding` port (agents.unifiedConfig fallback + agentPresetBindings styleTier stay LOCAL). traderton 65/0, herobids resolver 19/0. CodeReviewer PASS (MEDIUM#1 schema-dead venueFamily-falsy micro-coverage — noted in 004, non-blocking). See 004/005 "D1-c2".
    - [x] **D1-c3 — startup-context venue read (#3). DONE — REMOVED entirely (not re-pointed).** GROUNDED (2026-09-12): `apps/worker/src/startup-context.ts` (`resolveBotStartupContext`) was dead-but-exported+tested — ONLY its own test referenced it; NO live worker/api caller. The live grant path lives elsewhere (`index.ts` `approvalVenueAccountResolver` + `apps/api/routes/bots.ts` forwarding connectionId → Traderton resolves the account). Traderton deliberately DELETED the equivalent grant front-end in Phase 8 (create-trading-runtime.ts §424, decisions 11–13). Decision agent ruling B (settled by parity): REMOVE the file + its test entirely — re-pointing would AUTHOR a boundary tool to feed a function nobody calls, reconstructing a retired abstraction (violates copy-never-author). Removes the `venue_accounts`/`bot.venueAccountId` compile-dep ahead of D1-c4. herobids worker+api tsc green; worker suite 3059/0. Recorded: 004 "D1-c3", 001 Intentional-divergence. See 004/001.
    - [ ] **D1-c4 — terminal drop WAVE (decomposed 2026-09-12 via full reader audit — Contemplator). NOW 9 sub-slices + 2 findings.** The original "three readers" the D1-tables ruling named were the WORKER path (all DONE = c1/c2/c3); a full `apps/api` audit found a DOZEN MORE in-process readers/writers of the trading tables + **two findings: (F1) `positions` is a SIXTH trading table (boundary-owned; verbatim `PositionRepository.loadAgentPositions` copied Traderton-side + `get_agent_positions` reads it) missing from the drop-set — must drop with the five and re-point its readers identically to `fills`; (F2) `capabilities/trading.ts` (largest live reader: agent state/activity/outcomes/positions UI) + `accounts.ts POST /venue-accounts` (a live `venueAccounts` WRITE, not just a read — documented transitional isolation violation) were missing/mis-framed.** Sequencing law: author boundary read → re-point consumer → remove ruling-5 fallback → drop table LAST (never a broken intermediate). Sub-slices:
      - [x] **c4.1 — agent-scoped API re-points, USER subject (REUSE existing c1 tools). DONE.** `capabilities/trading.ts {state,activity,outcomes,positions}` re-pointed onto `get_agent_fills`/`get_agent_journal_events`/`get_agent_positions` (in-app aggregation; `/positions` exit-price reconstructed in-app; scoping moved connectionId→creatorId per settled ruling 004 "c4.1") + `agent-interactivity.ts /agents/:id/trades` → `get_agent_fills` (+ unit tests). Subject actor `{type:'agent',id:agentId}` (c1 scopes server-side by agentId); unconfigured boundary → 503 precondition.not_ready. api tsc clean; c4.1 unit 60/0 (trading 17 + agent-interactivity trades 3 + existing); exports 42/42. CodeReviewer PASS (no CRITICAL/HIGH; L1 wasted-fetch FIXED in-slice; M1/M2/L2/L3 → Outstanding, non-blocking). Wiring: `tradertonBotClient` threaded into capabilityRoutes + agentInteractivityRoutes in index.ts. **RE-SCOPED (2026-09-12): `analytics.ts` + `blueprint-performance-scorer.ts` MOVED to c4.2** — analytics is USER/owner-scoped + reads `bots.config` metadata (no agent-scoped c1 tool covers it); blueprint-scorer is a SERVICE needing a SYSTEM-subject agent-scoped positions read + wiring through 5 fire-and-forget call sites. Both need c4.2 owner/system-scoped reads, not the c1 USER-subject agent tools.
      - [ ] **c4.2 — owner/bot-scoped + SYSTEM-subject read tools + export re-point (011's "separate export slice that blocks the drop"). DECOMPOSED into mini-slices:** existing owner family = `list_owner_bots`/`get_owner_bot_status`(returns config)/`get_owner_bot_costs`/`get_owner_bot_sessions`/`get_owner_bot_journal`/`get_owner_bot_journal_summary`. MISSING (author): raw owner/bot-scoped fills+positions.
        - [x] **c4.2-tools — DONE.** Authored 5 boundary read tools in traderton `bots.ts`: `get_owner_bot_fills` + `get_owner_bot_positions` (single owned bot, `getBotByIdForOwner` gate → not_found.resource) + `get_owner_fills` + `get_owner_positions` + `get_owner_journal` (all owner bots via `getBotsByOwner`, empty→[]). Copied verbatim from herobids exports.ts route queries; ownership re-keyed to owner-scoped repo methods. category read-database (reachable over boundary with NO further registration — keyed off category, not a name allowlist). worker build+tsc clean; bots.test 92/92 (29 new). Recorded in 005. CodeReviewer PASS (no CRITICAL/HIGH/MEDIUM; 3 LOW → Outstanding).
        - [x] **c4.2-exports — DONE.** Re-pointed `exports.ts` bot-level (`/bots/:id/export/{trades,journal,config,report,bundle}`) + account-level (`/export/{trades,bundle}`) onto the c4.2 tools (config via `get_owner_bot_status`). USER subject `{ownerId,actor:{type:user}}`. 404-preservation: added `details.errorCode` unwrap to `exports-traderton.ts mapClientResultToReadResult` (mirrors bots.ts) + `sendLoadError` maps not_found.resource→404 `{error:'not_found'}`; genuine validation.invalid_payload still →502. Empty-case parity (header-only CSV/[]) via `get_owner_fills`→{fills:[]}. Journal from/to filtered in-app (tool has no date param — TOOL GAP, in-app filter is inclusive-parity). Unconfigured→503. Added `loadBoundaryObject` seam helper + extracted `mapNonSuccessToError` (loadAgentEvidence unchanged). Functional helper wired with stub client + tool stubs. api tsc clean; exports 58 + seam 8 = 66/0; consumer suites 60/0. CodeReviewer PASS (no CRITICAL/HIGH/MEDIUM; all 9 focus areas confirmed; 3 LOW — 1 comment-refresh FIXED in-slice).
        - [x] **c4.2-views — DONE.** Re-pointed `views.ts` `/bots/:botId/positions` + `/positions/open` → `get_owner_bot_positions` (open filtered closedAt===null in-app); `/journal` → `get_owner_bot_journal({botId:actorId,...})` (USER subject). The `/journal` re-point TIGHTENS scope: it now owner-verifies the bot actorId (sole live consumer = InstanceDetailPage passing a bot id) → unowned/absent → 404, making the endpoint's "prevent cross-user leaks" contract TRUE (old query only required actorId present, not owned). Added views.test.ts (8/0). CodeReviewer PASS (no CRITICAL/HIGH). **Fixed 2 MEDIUM in-slice: (M1) added `orderBy(desc(updatedAt))` to traderton `get_owner_bot_positions` to reproduce herobids `getAllByActor` ordering the `/positions` view relies on; (M2) `type || undefined` so herobids' allowed empty `type:''` doesn't hit the tool's `.min(1)` → 502.** herobids api tsc + views 8/0; traderton worker build+tsc + bots 92/0.
        - [ ] **c4.2-misc → SPLIT (2026-09-12):**
          - [x] **c4.2-health — DONE.** `actor-health.ts /bots/:id/health` re-pointed → `get_owner_bot_status` (via loadBoundaryObject; static-fallback status mapping running→degraded preserved; runtime-snapshot Redis path unchanged; not_found→404; unconfigured→503). `/agents/:id/health` stays LOCAL (`agents` platform table). Added actor-health.test.ts (4/0). api tsc clean. CodeReviewer PASS (no CRITICAL/HIGH; 2 trivial LOW).
          - [x] **c4.2-recon — DONE.** Authored traderton `get_owner_bot_reconciliation_events` (`{botId,limit?,offset?,since?}` → guards → `getBotByIdForOwner` gate → resolve bot venueAccountId via direct ctx.db select on the owner-verified bot → `ReconciliationEventRepository.getByVenueAccount` → `{ok,botId,venueAccountId,events}`). Re-pointed herobids `reconciliation.ts` onto it (USER subject; not_found→404; 400-validation; unconfigured→503). This makes `reconciliation_events` (a traderton trading table) boundary-owned; herobids `ReconciliationEventRepository` now has ZERO live importers → drops with c4.9. traderton worker build+tsc + bots 98/0 (+6); herobids api tsc + reconciliation 5/0. Functional stub added. CodeReviewer PASS (no CRITICAL/HIGH; M1 validation-before-ownership ordering delta → 003 + Outstanding; 3 LOW).
        - [x] **c4.2-analytics — DONE (decision-agent ruling C, settled by parity — 004/no-divergence).** Extended traderton `get_owner_journal`/`get_owner_positions` with ADDITIVE optional `{botIds?,from?,to?,limit?}` (botIds INTERSECTED with owner bots — never widens; backward-compatible with the no-arg /export/bundle callers; journal time on createdAt + desc order, positions time on closedAt; limit threaded). Re-pointed herobids `computeAnalytics`: bot config metadata via `list_owner_bots` + per-bot `get_owner_bot_status` (RAW config — NOT the lossy strategyPreset — so decisionModes/executionModes filters keep working; step-3 agent expansion derives from list_owner_bots creatorType/creatorId, no extra call); journalEvents→`get_owner_journal({botIds:targetBotIds,from,to,limit:10000})`; positions→`get_owner_positions(...)`. Aggregation (groupBy/session/PnL) kept VERBATIM; `agents`/`agentRuntimeSessions` stay LOCAL. N+1 metadata is fail-loud (a get_owner_bot_status failure aborts with {ok:false}). USER subject; unconfigured→503. traderton bots 106/0 (+8); herobids analytics 34/0 + exports/views 66/0. CodeReviewer PASS (no CRITICAL/HIGH; 1 MEDIUM test-assertion-strengthening FIXED in-slice; 2 LOW). No parity divergence (10k cap + time filter preserved server-side).
        - [x] **c4.2-scorer — DONE.** `blueprint-performance-scorer.ts` re-pointed: single `get_agent_positions({})` boundary call (subject `{ownerId:bp.authorId, actor:{type:agent,id:agent.id}}` — mirrors c1 evidence-assembler) replaces BOTH the local `bots` botId read AND the closed-`positions` read; filters closedAt!==null in-app; metric math VERBATIM. `blueprints`/`agents` reads + UPDATE stay LOCAL. Read client threaded through blueprints.ts (6h loop) + agents.ts (start/stop) + agent-lifecycle-service (startAgent/stopAgent) + agent-blueprint-sync-service (ensurePublishedBlueprintForAgent + 3 helpers) from index.ts. **BEST-EFFORT SKIP divergence (003 recorded):** unconfigured client OR boundary read failure → SKIP (no overwrite, warn) rather than 503/zero — it's a fire-and-forget recompute; telegram start/stop passes undefined (recompute via 6h loop + web/API). scorer 7/0; services+blueprints 153/0; agents+telegram 138/0; api tsc clean. CodeReviewer PASS (no CRITICAL/HIGH; M1 ledger-entry ADDED to 003; 2 LOW). **← c4.2 WAVE COMPLETE (tools/exports/views/health/recon/analytics/scorer all DONE).**
      - [ ] **c4.3 — Q4 `/credentials` removal ENTIRELY (human-ratified, 004 D1-cred-Q4). SPLIT into api + web.** GROUNDED scope map (2026-09-12):
        - [x] **c4.3-api — DONE.** Deleted `credentials.ts` + `credentials.test.ts` + its `index.ts` registration; delete `credential-dependents.ts` (ONLY importer is credentials.ts, confirmed); remove `CreateCredentialSchema`/`RotateCredentialSchema` from `schemas.ts` (if used only by credentials.ts — verify); the `credentialCreatedEvent`/`credentialRotatedEvent`/`credentialDeletedEvent` domain events stay in `@herobids/domain` (just stop importing here); remove ONLY the `user_credentials` label/provider DISPLAY subqueries in `connections.ts selectConnectionView` (pure read display — safe/clean). **REFINED c4.3/c4.4 boundary (2026-09-12): BOTH the `accounts.ts POST /venue-accounts` credentialId flow AND the `connections.ts` connection-create credentialId-VALIDATION block are DEFERRED to c4.4** — they are entangled with the venue-account write + connection grant flow (connections.credentialId links the credential model being dismantled); a partial edit risks an unvalidated intermediate. So c4.3-api removes only: credentials.ts route + test + credential-dependents.ts + registration + schemas + the connections.ts credential DISPLAY subqueries. accounts.ts + the connections.ts credentialId-validation block → c4.4. The `userCredentials` drizzle import survives in both files until c4.4; `user_credentials` table drops at c4.9. — accounts POST is a c4.4 venue-write re-point; touching only part of it now risks an inconsistent intermediate. So c4.3-api leaves accounts.ts untouched (its `userCredentials` import survives until c4.4; `user_credentials` table drops at c4.9 regardless). `checkCredentialLimit` already re-points to platform_credentials (D1-cred) — leave it.
        - [ ] **c4.3-web (SCOPE WIDENED per human 2026-09-12: remove BOTH CredentialsPage AND VenueAccountsPage — both are trading-specific frontend surfaces; part of the planned "remove trading from the frontend" feature set, informed by the legal-isolation objective. ConnectionsPage STAYS — it's the platform grant layer, NOT trading-specific).** Remove CredentialsPage + VenueAccountsPage (pages + `/credentials` & `/venue-accounts` routes + `nav.credentials` & `nav.venueAccounts` sidebar + their tests + credentials.* & venueAccounts.* i18n across en/hi/ar). RELOCATE `PROVIDER_TEMPLATES` (currently exported from CredentialsPage.tsx; imported by the SURVIVING `features/setup/ProviderSetupForm.tsx` + 2 tests) into a standalone module (e.g. `features/credentials/credentials-templates.ts` or fold into setup) BEFORE deleting the page; delete `features/credentials/CredentialsPage.tsx` + its 3 tests (or retarget the PROVIDER_TEMPLATES test to the new module); remove the `/credentials` route + sidebar/nav entry + `credentials` object in `lib/api-client.ts` + i18n `nav.credentials` + `credentials.*` keys + the AgentCapabilityPage `/credentials` link. Keep ProviderSetupForm working.
        - [x] **c4.3-web — DONE (both pages removed).** Removed CredentialsPage + VenueAccountsPage entirely: pages + `/credentials` & `/venue-accounts` routes + `nav.credentials` & `nav.venueAccounts` sidebar + their tests + `credentials.*`/`venueAccounts.*`/`nav.*` i18n across en/hi/ar + `agents.capabilityPage.manageCredentials`. RELOCATED `PROVIDER_TEMPLATES` → new `features/setup/provider-templates.ts` (+ moved/retargeted its cross-surface alignment test); re-pointed ProviderSetupForm. api-client: removed `credentials`+`venueAccounts` objects + dead `Credential`/`VenueAccount`/`VenueProfile` types + `credentialLabel`/`credentialProvider` from `Connection`. ConnectionsPage KEPT (platform grant layer, not trading) — only dropped the credential field render + guard. AgentCapabilityPage kept `manageConnections`, dropped `manageCredentials`. FIXED 3 LOW in-slice (redundant ConnectionsPage guard collapsed; duplicate test name renamed; dead `connections.credential` i18n key removed from all 3 locales). Web typecheck clean; web vitest 672/0 (165 suites). CodeReviewer PASS (no CRITICAL/HIGH). **← c4.3 COMPLETE (api + web).** Rationale (human): both are trading-specific frontend surfaces, part of the planned "remove trading from the frontend" set informed by legal isolation.
      - [~] **c4.4 — local venue-account WRITE removal + connection credentialId + provisioner removal. IN PROGRESS.** DECOMPOSED:
        - [x] **c4.4-venue-accounts — DONE (decision-agent ruling A, settled within the rules — no divergence).** REMOVED `venueAccountRoutes` (`POST`/`GET`/`DELETE /venue-accounts`) ENTIRELY rather than re-pointing: caller audit (both coordinator + decision agent) found NO live caller — the web `venueAccounts` api-client object was deleted in c4.3-web, no worker/other-route caller. Deleted `apps/api/src/routes/accounts.ts` (whole module; `venueAccountRoutes` was its only export — carried the local `db.insert(venueAccounts)` WRITE [the forbidden coupling], the local `venue_accounts` GET read, and the already-boundary-re-pointed DELETE) + `accounts.test.ts`; removed the import + registration from `index.ts`; removed `CreateVenueAccountSchema` + `CreateVenueAccountInput` from `schemas.ts` (used only by the deleted POST). Capability-preserving: CREATE persists via setup→`provision_venue_account` (setup.ts createProviderLink Phase 1); DELETE persists via DIRECT `deprovision_venue_account` calls in setup.ts:492 (compensation) + provider-links.ts:192 (guided teardown) — neither routed through the deleted HTTP route. Also resolves the deferred c4.3 accounts.ts `userCredentials` credentialId-validation branch (died with the POST). LOW housekeeping done in-slice: removed stale `Disallow: /credentials` + `/venue-accounts` from `apps/web/public/robots.txt` (both frontend routes removed in c4.3-web). api tsc clean; api routes+plan-guards suite 1117/0. CodeReviewer PASS (no CRITICAL/HIGH; 1 LOW = robots.txt, fixed in-slice). **NOTE — reframes the original c4.4 line:** the "re-point POST → provision_venue_account manual mode" plan is SUPERSEDED (the POST is dead → removed, not re-pointed); `provision_venue_account` manual mode already serves the live setup flow.
        - [x] **c4.4-connections — DONE (decision-agent per-coupling ruling; gate: settled-within-rules for the removals, sequencing-law defer for the rest).** THREE couplings triaged: **(A) display subqueries** — REMOVED `venueAccountLabel` + `venueAccountVenue` from `selectConnectionView` (dead in the only consumer [web ConnectionsPage renders neither] + local reads of trading-owned `venue_accounts`; mirrors c4.3 credential-display removal). KEPT `venueAccountRef` (IS rendered = funding address) → **DEFERRED**: must be re-pointed over a boundary venue-account read BEFORE the `venue_accounts` drop at c4.9. `referencingBotCount` kept (rides with (C)). **(B) credentialId validation** — REMOVED the `userCredentials` existence/provider-match read + BOTH now-dead `23503→credential.not_found` FK-catch remaps (connections.credentialId has NO FK — schema confirms only userId→users; post-Q4 nothing writes `user_credentials` from herobids → validation was structurally dead, could only spuriously 400) + orphaned `userCredentials`/`credentialMatchesConnectionProvider` imports. KEPT `credentialId` column + `providerRequires/Allows/SupportsCredential` policy 400s (platform policy). Ledger: Intentional-divergence (a non-null credentialId no longer 400s on a dead lookup — improvement). **(C) connection hard-DELETE bot-guards** (`blockingBots`/`concurrentBots` reading local `bots` → 409 connection.in_use) — **DEFERRED to c4.9**: cannot re-point (traderton bots have NO connectionId, decision 13 — authoring one is forbidden), cannot remove (degrades a behaviour-bearing invariant / breaks intermediate while `bots.connectionId ON DELETE RESTRICT` still local). Reconciled connections.test.ts (removed 3 dead credential-validation tests incl. FK-race; narrowed 3 display tests + key-set assertion to venueAccountRef; simplified buildMockDb). api tsc + web typecheck clean; connections suite 31/0; web ConnectionsPage 22/0. Web LOW folded in: removed stale `venueAccountLabel/venueAccountVenue` from web api-client connection type + ConnectionsPage.test fixture. CodeReviewer PASS (no CRITICAL/HIGH/MEDIUM; LOW web-type FIXED in-slice; LOW stale dist artifact = gitignored, ignore). **⚠ HUMAN PRODUCT QUESTION FLAGGED (non-blocking, does NOT gate the loop):** post-Q4, should `POST /connections` VALIDATE `credentialId` against `platform_credentials`, or is `credentialId` on the primitive endpoint now vestigial (only the setup flow writes it)? Recorded for later human input; loop continues (only merge-to-main is a hard stop).
        - [ ] **c4.4-connections-followups — DEFERRED (carried).** (1) `venueAccountRef` display subquery in `selectConnectionView` → re-point over a boundary venue-account read before c4.9. (2) connection hard-DELETE `bots` guards + `referencingBotCount` → c4.9 (bots-drop): resolve the successor invariant "what enforces connection↔bot teardown safety after `bots` drops" (traderton has no connectionId; only a venue-account-scoped boundary guard `deprovision_venue_account`→provision.in_use exists).
        - [x] **c4.4-provisioner — DONE.** DELETED `apps/api/src/trading-provisioner.ts` — re-audited (grep whole repo ex-dist): `provisionTradingTarget` had ZERO live callers (only its own definition), no test, fully self-contained (only export + file-local types). It carried a local `tx.insert(venueAccounts)` WRITE + `connections.resolvedVenueAccountId` update — a dead isolation-violating write path. api tsc clean. **← c4.4 COMPLETE (venue-accounts + connections + provisioner; the credentialId-column-vs-platform_credentials product question is flagged for human, non-blocking).** REMAINING residual live `venue_accounts` READS gated to their owning slices (NOT c4.4): `blueprints.ts:2106`, `dashboard.ts:154,293` + the deferred `connections.ts venueAccountRef` subquery — all re-point/remove before the c4.9 drop.
      - [x] **c4.5 — DONE (decision-agent per-concern ruling; gate: settled by parity / within-rules for every concern, autonomous ruling D).** `bots.ts` made a PURE boundary consumer of the `bots` table — no local `bots` read/write remains (grep-confirmed: no `.from(bots)`/`db.delete(bots)`/`db.update(bots)`). **(Q1)** removed ALL ruling-5 local read fallbacks (`resolveBotForLifecycle`, GET `/bots`, GET `/bots/:id`, and the 5 sub-resource existence checks costs/sessions/events/journal/journal-summary) → boundary MANDATORY (B7 fail-fast): absent/transport/in_progress → typed 503 precondition.not_ready; genuine tool fault → 502; not_found.resource → 404 (readBoundary unwraps the tool errorCode from the validation.invalid_payload wire code). **(Q2)** removed the DELETE `/bots/:id` trailing local `db.delete(bots)` mirror (boundary `delete_bot` already authoritative; inert once reads are boundary-only; greenfield). **(Q3a)** PATCH `/bots/:id/config` existence read re-pointed → `get_owner_bot_status` (returns status/config/creatorType/creatorId — ruling-4 additive divergence). **(Q3b)** removed the local `db.update(bots)` config write + made `adjust_bot_config` UNCONDITIONAL (dropped the `status==='running'` guard — grounded: adjust_bot_config persists config for ANY status via updateBotConfig, enforces mode-escalation internally; ownership round-trips since boundary-created bots are stamped creatorId=userId). **(Q4)** POST `/bots/:id/blueprints` local `bots` read re-pointed → `get_owner_bot_status` (uses data.config for the projection). Behaviour-preserving deviation (CodeReviewer-verified equivalent): PATCH venue-type check now derives from boundary `config.venue` (stamped at create via normalizeBotConfig from the same provider token) instead of a local `connections.provider` read keyed on connectionId (which get_owner_bot_status doesn't return). Removed orphaned imports (bots/fills/journalEvents/sql/sum/asc/inArray/PgJournal) + `hasReadBoundary`. Kept: create/stop/start action routing, mode-escalation, plan gates, agent-notify xadd, platform connections/blueprints reads. api tsc clean; bots suite 26/0 (11 create preserved + 15 new boundary-only, incl. a no-table db mock that throws on select/update/delete). CodeReviewer PASS (no CRITICAL/HIGH/MEDIUM; L1 recorded below; L2 test-comment FIXED in-slice). **Outstanding (LOW, carried):** L1 — create does NOT server-side enforce `venue===conn.provider` (client-stamped per bug-report 004); a cross-venue-family mismatch would make the new PATCH venue-type check diverge from the old connections.provider behaviour. Minimal risk (coarse swap-vs-orderbook mapping, already client-controllable pre-change, boundary re-validates). Note if venue↔provider consistency is ever tightened.
      - [x] **c4.6 — DONE.** Added a focused functional test isolating the DELETE `/bots/:id` `bot.running`→409 BACKSTOP (distinct from the pre-gate 409): `get_owner_bot_status` returns status='stopped' (pre-gate PASSES → delete_bot IS invoked), but the authoritative `delete_bot` returns `kind:'failure'` with top-level `details.errorCode:'bot.running'` (race: bot started between gate read and delete) → handler maps to 409 via `errorPayload('bot.running', ...)`. Asserts 409 + `error==='bot.running'` + delete_bot was invoked (proves it reached the backstop, not the pre-gate). Confirmed the two 409s take different code paths (invokeBoundary returns the raw TradertonClientResult verbatim so the handler reads `deleteResult.details.errorCode` directly — unlike readBoundary which unwraps). api tsc clean; bots suite 27/0 (was 26). CodeReviewer PASS — caught + FIXED a MEDIUM in-slice (an `as unknown as TradertonClientResult` cast violating the AGENTS.md ban; replaced with a structurally-conforming failure literal incl. correlationId).
      - [x] **c4.7 — DONE (decision-agent per-coupling ruling; gate: settled within the rules, autonomous). MECHANISM REFINED from the original "re-point" note: REMOVE, not re-point.** GROUNDED: BOTH admin `bots` couplings are CROSS-TENANT reads the owner-scoped boundary structurally cannot express (subject model is `{ownerId,actor}`; even the `system` actor carries a concrete ownerId — no cross-owner hole by design, per L3-Q2-P). (A) `/admin/stats` `count(bots)` = platform-wide total (no single owner); (B) `/admin/users` per-user `botCount` subquery = admin reading EVERY listed user's (other owners') count. `count_venue_accounts` is OWNER-scoped → not a usable template here. Re-pointing would require AUTHORING a cross-tenant count tool + a cross-owner authorization affordance in the trading authority — REJECTED per the c4.2-analytics precedent (platform aggregation not authored into Traderton) + it would puncture the boundary's deliberate no-cross-owner-hole invariant. Both counts are DISPLAY-ONLY observability tiles (nothing gates on them), greenfield, NOT inventoried capabilities. So: REMOVED the `bots:` tile from `/admin/stats` counts (+ the `count(bots)` Promise.all query + `botCount` destructure var) and the per-user `botCount` subquery from BOTH `/admin/users` select branches; `agentCount` (platform `agents` table) KEPT; removed the orphaned `bots` import. The pre-ruling's DIRECTION (no in-process trading read; §3.1) is honoured — mechanism is remove, reaching the same end-state without the rejected authoring. NO Traderton tool authored. api tsc clean; admin suite 25/0 (fixed the positional-count mock: dropping the bots slot shifts the 2nd count to agents). CodeReviewer PASS (no CRITICAL/HIGH/MEDIUM; positional-count alignment verified 6↔6; LOW stale comment FIXED in-slice). **Ledger: TWO Intentional-divergences (below).**
      - [x] **c4.8 — DONE (decision-agent ruling Option A; gate: settled by parity, autonomous). Option B (metering relocation) confirmed a RED HERRING — nothing meters off `fills` (usage billing reads `usage_events`/`billingPeriods`; `/trading/fills` is a pure display ledger), so no product call / no human escalation; the 004 metering concern stays Deferred (no live behaviour to preserve).** Re-pointed herobids `GET /trading/fills` (billing.ts) OFF the local `fills` table ONTO an additively-extended owner-scoped boundary tool (c4.2 additive pattern). TRADERTON: extended `get_owner_fills` with `botIds` (intersect owner bots), `creatorAgentId` (restrict owner bots to creatorType=agent+creatorId — reproduces old agentId-mode: fills under the agent's BOT actors), `agentIds` (UNION agent-actor fills via `or()`; caller-supplied+ownership-verified since Traderton has no agents table), `limit`/`offset` (`orderBy desc(filledAt)` tied to limit-presence; offset only when present); no-arg `/export/trades` caller BYTE-IDENTICAL (bot-actor fills, unordered, unlimited). Also extended `get_owner_bot_fills` with `limit`/`offset` (same pattern). HEROBIDS modes: botId→`get_owner_bot_fills` (server-side ownership; not_found.resource→404 `billing.ledger.bot_not_found` — NO local bots read); agentId→keep LOCAL platform `agents` 404-guard (`billing.ledger.agent_not_found`) then `get_owner_fills({creatorAgentId})`; owner-wide→resolve owner agent ids locally (platform `agents`) as `agentIds`, tool resolves bots server-side + unions. Boundary absent/transport/in_progress→503. Reused the exports-traderton `createTradertonReadBoundary`/`loadAgentEvidence`/`toFillRow` seam; threaded `tradertonReadClient`+timeout into billingRoutes (index.ts passes tradertonBotClient). Removed billing.ts local `fills`/`bots` imports + unused drizzle ops. LOW-1 (pre-existing NaN limit/offset guard) FIXED in-slice (mirrors usage-events clamp). traderton worker tsc+build clean, bots suite 117/0; herobids api tsc clean, billing suite 23/0. CodeReviewer PASS (no CRITICAL/HIGH/MEDIUM; no-arg parity + scope-safety + old-agentId semantics + 404/503 all verified; 3 LOW, LOW-1 fixed). No local `fills` read remains in billing.ts → clears one c4.9 fills-drop precondition. Parity: Preserved (re-point) + server-derived scope Intentional-divergence (per c4.2/c4.5).
      - [ ] **c4.9 — DROP the tables (LAST). ⚠ RE-SCOPED (2026-09-12 audit): the drop is blocked by a SUBSTANTIAL remaining re-point wave — the backlog wrongly assumed all readers were already gone. A grep of actual runtime queries (`.from/.insert/.update/.delete(<table>)` + raw `FROM <table>`) across `apps/**/src` (ex-tests) found these LIVE trading-table accesses that MUST be re-pointed/removed BEFORE the drop (sequencing law). DECOMPOSED into c4.9a–c4.9f below; each grounded, several are four-risks needing decision-agent routing + possibly new/extended boundary tooling. Type-only `typeof <table>.$inferSelect` imports + local vars named `fills`/`positions` + `*.json` artifact names are NOT blockers.**
        - [x] **c4.9a + c4.9e — DONE together (decision-agent per-guard ruling; gate: settled within the rules; recommended path taken, NO escalation).** Resolved the c4.4-connections (C) deferred successor invariant: **the venue-account-scoped boundary guard IS the successor** — connection↔bot collapses into connection→resolvedVenueAccountId→bot (traderton bots have NO connectionId, decision 13; a connection with a null resolvedVenueAccountId has no trading bots). Authored TWO copy-faithful owner-scoped read tools in traderton `provisioning.ts` (registered in `provisioningTools`): `count_bots_by_venue_account({venueAccountIds})` (batch; VERBATIM deprovision predicate `bots WHERE venueAccountId IN inScope`, no status filter; narrows to owner's in-scope accounts first — unowned OMITTED, never leaked) + `get_venue_account({venueAccountId})` (owner-scoped; PUBLIC display fields only — venueAccountRef/venue/label, never credentialId/venueProfile; absent→not_found.resource). **c4.9e (provider-links.ts):** removed local `connBots`/`venueAccountBotIds` reads — duplicated the authoritative `deprovision_venue_account` provision.in_use guard already in `deleteProviderLink`'s path; now blocks on active agent grants (local platform) → boundary deprovision (authoritative bot guard→blocked) → local delete. `connectionBotIds`/`venueAccountBotIds` fields kept always-[] (wire compat). **c4.9a (connections.ts):** threaded `tradertonClient`; hard-DELETE bot guard → `count_bots_by_venue_account` ONLY for non-null resolvedVenueAccountId (null=vacuous skip); removed `referencingBotCount`+`venueAccountRef` SQL subqueries from selectConnectionView, re-populated per-row via ONE batched count call + per-account get_venue_account. Boundary-absent posture: display degrades (count→0/ref→null, list stays up), hard-delete + provider-link delete FAIL CLOSED (503, never delete without the guard). Wire shapes preserved (409 connection.in_use, provider_link blocked, view keys). index.ts wiring updated. traderton worker tsc+build clean, tools suite 138/0; herobids api tsc clean, connections+setup 65/0. CodeReviewer PASS (no CRITICAL/HIGH/MEDIUM; copy-faithfulness + owner-scope + teardown-safety + fail-closed-on-mutation all verified; 4 LOW = empty bot-id arrays in blocked payload [decision-13 tradeoff], 502-vs-503 cosmetic, batch-get deferral, view typing — all accepted). Two new tools → 001 read-seam entries. connections.ts + provider-links.ts now have ZERO local bots/venue_accounts reads (grep-confirmed).
        - [x] **c4.9b — DONE (decision-agent per-read ruling; gate: PASS all reads; in-app assembly, NO aggregation authored Traderton-side per c4.2).** dashboard.ts `/overview` + `/activity` re-pointed OFF local bots/venue_accounts/positions/journalEvents ONTO owner/agent-scoped boundary reads + in-app assembly (mirrors analytics.ts seam): bots→`list_owner_bots`; venue label→`get_venue_account` per distinct id; open-position count→`get_owner_positions` (filter closedAt==null, group in-app); last-activity→`get_owner_journal` (max in-app); two-part PnL→`get_agent_positions` per agent (folds agent-direct + agent-owned-bot; sum in-app); activity feed→`get_owner_journal({botIds,limit})` (keyset cursor + hasMore + classifyEvent in-app). `/agent-activity` UNCHANGED (platform-only). ONE copy-faithful ADDITIVE traderton extension: `list_owner_bots` += venueAccountId/startedAt/stoppedAt, `get_owner_bot_status` += venueAccountId, `ToolBotRecord` += venueAccountId (surfacing existing full-row columns — same class as shipped creatorType/creatorId; agent-scoped list_bots/get_bot_status UNCHANGED; @traderton/domain rebuilt). Boundary-absent → 503; get_venue_account not_found→empty labels, transport/failure→PROPAGATE (M1 fix in-slice). Response shapes byte-preserved. dashboardRoutes signature += (readClient, timeout); index.ts wired. Removed dashboard.ts local table imports + orphan drizzle ops. NEW dashboard.test.ts 7/0; traderton bots suite 118/0; both tsc+build clean; grep confirms ZERO local bots/positions/journalEvents/venueAccounts reads in dashboard.ts. CodeReviewer PASS (no CRITICAL/HIGH; **PnL fold is a CORRECTNESS IMPROVEMENT** — old connectionId-join could double-count a bot shared by N agents, new creatorId scoping fixes it; M1 venue-read-failure-classification FIXED in-slice; M2 N+1 venue reads = existing pattern; L1 stale comment untouched code; L2 config.venue-fallback drop = noted divergence). Ledger: re-point (parity preserved) + additive tool extension (001/005/006) + Intentional-divergences (c4.1 connectionId→creatorId PnL basis; config.venue display fallback drop).
        - [x] **c4.9c + c4.9d (reads) + c4.9g — DONE (decision-agent per-read/per-write rulings; gate: settled within rules for the reads; F/G bot-instantiation WRITE escalated + deferred). 2026-09-12.** Authored copy-faithful traderton `DecisionRepository.loadAgentDecisions(agentId,{from,to,limit})` (agent-native + agent-owned-bot fold via inlined `bots WHERE creatorType='agent' AND creatorId=agentId`; two parallel selects concat; sort desc(createdAt); slice(limit); batch execution_plans latest-status attach — no route N+1) + worker tools `get_agent_decisions`/`get_agent_decision_failures` (in `botManagementTools`; latter wraps existing `DecisionFailureRepository.query`) + `count_bots_by_blueprint({blueprintIds})` in `provisioningTools` (mirrors count_bots_by_venue_account, owner-scoped narrow). HEROBIDS re-points: (A) `/agents/outcomes` → per-agent `get_agent_positions` + in-app counts/PnL (Promise.all all-or-nothing parity); (C) `/agents/:id/decisions` → `get_agent_decisions`; (D) `/agents/:id/decision-failures` → `get_agent_decision_failures`; (B) agent-delete cascade → boundary teardown FIRST (list_owner_bots→filter creatorType=agent+creatorId→stop_bot-if-running→delete_bot, fail-closed 503; removed local `db.delete(bots)` + `db.update(venueAccounts).set({credentialId:null})`); (E) blueprint-delete dep-check → `count_bots_by_blueprint` hoisted before tx, fail-closed 503; (H) telegram `/log` decision-failures leg → `get_agent_decision_failures` (degrades, never 503). Threaded client into telegramWebhookHandler (agent-interactivity.ts + index.ts). VERIFIED: herobids api tsc clean + agents/blueprints/telegram/agent-interactivity 244/0; traderton worker tsc + db/worker builds clean + bots/deprovision/agent-evidence-loaders 171/0. CodeReviewer: round-1 1 HIGH (H1 outcomes attribution divergence) + 3 MED + 3 LOW → round-2 PASS (no CRITICAL/HIGH/MEDIUM, 2 LOW). H1 RESOLVED by decision-agent ruling (a): the connectionId→creatorId rescope is settled within rules (decision 13 + c4.1; connections has no Traderton successor) → Intentional-divergence in 001, NO attribution code change. M1 (serial→Promise.all all-or-nothing), M2 (handleLog tests), M3 (decisions serialization-parity tests), L1 (limit comment), L2 (vestigial connections read removed) all FIXED. Parity: re-point preserved + server-derived scope Intentional-divergences (001). **⚠ REMAINING for c4.9d: the blueprint→bot INSTANTIATE write (F/G) is DEFERRED — see below.**
        - [ ] **c4.9c-original-note — agents.ts. ⚠ SCOPE-EXPANDING FINDING (2026-09-12, deeper audit): the trading-table set is LARGER than the 6 this c4.9 decomposition assumed.** The schema comment (herobids `agent-messages.ts`) states "Trading truth remains in decisions, plans, fills, positions, and journal tables" and the TRADERTON schema carries ~14 trading tables incl. `decisions`, `orders`, `execution_plans`, `decision_failures`, `llm_decision_artifacts`, `balance_snapshots`, `decision_contexts`, `instruments`, `token_safety_overrides`, `market_assessment_*`. agents.ts reads BEYOND the 6-table set: (a) `/agents/outcomes` (~730-820) — per-agent trading PnL + open/closed/winning position COUNTS via the two-part positions+bots+agentConnections aggregation (a richer read than dashboard's sum; re-point over get_agent_positions per agent + in-app metric compute, c4.2 pattern — DOABLE with existing tools). (b) agent-delete cascade `db.delete(bots)` (~1735, LOCAL bot-delete WRITE) + `db.update(venueAccounts).set({credentialId:null})` (~1763, LOCAL venue-account WRITE) — needs a boundary agent-cascade delete (four-risk). (c) **`/agents/:id/decisions` (~2022) reads local `decisions` + `execution_plans` (status subquery), scoped by local `agentBots` — NO boundary decisions-read tool exists.** (d) **`/agents/:id/decision-failures` (~2189) reads local `decision_failures` — NO boundary tool exists.** These (c)/(d) + `telegram-command-handlers.ts:289` (`decision_failures`) are a NEW family of drop-blockers on the WIDER trading-table set, requiring authoring `get_agent_decisions`/`get_owner_decisions` + a decision-failures read + execution-plan status — boundary tooling that DOES NOT EXIST. **This exceeds the c4.9a–f decomposition scope + the 6-table assumption. FLAGGED to human before proceeding.** See D1-c / D1-c4 (which already anticipated "a DOZEN MORE in-process readers" — this confirms the wider set).
        - [x] **c4.9d — DONE. (i) blueprint-delete dep-check re-pointed to `count_bots_by_blueprint` (hoisted before tx, fail-closed 503). (ii)/(iii) blueprint→bot INSTANTIATE write now over the boundary — landed 2026-09-12: traderton `5054785` (instantiate_bot tool + insertStoppedBot repo method + tests), herobids `1e1a8947` (Shape-1 seam + deterministic idempotent actorId). CodeReviewer round-2 PASS (no CRITICAL/HIGH/MEDIUM; 2 LOW in Outstanding). The source single-tx atomicity split is a bounded Intentional-divergence (003/001). This was the SOLE remaining trading-table WRITE in herobids `apps/**/src`.** [historical F/G escalation note below]
          **[historical] IN PROGRESS (F/G — human APPROVED authoring; decision-agent design ruling SETTLED 2026-09-12).** The `venueAccounts` binding read (~2106/2138) + `tx.insert(bots)` (~2244/2277) are the blueprint→bot INSTANTIATE write — the SOLE remaining trading-table write in herobids `apps/**/src` (full audit: everything else re-pointed/removed; worker CLEAN). Human said "proceed" with authoring the stopped-create boundary write. **Decision-agent design ruling:**
          - **Tool:** author a NEW sibling `instantiate_bot` in traderton (leave `create_bot` UNTOUCHED — it is a copied parity surface: create-AND-start, agent-scoped). `instantiate_bot` = the drive-target create path (copied `tryCreateBotWithLimit` + config validation/mode-escalation/swap guards) WITHOUT the `tryMarkBotRunningWithLimit` + `enqueueLifecycle('start')` steps (stopped-create = deletion of the start steps, authors nothing new).
          - **Contract:** caller-supplied `actorId` (herobids pre-generated uuid; both sides must agree for retry convergence); `status:'stopped'`; `creatorType:'user'`/`creatorId:userId`; persist `blueprintId`/`blueprintRevisionId`/`configSnapshot`; caller-supplied `venueAccountId` (key to the SPECIFIC binding account, NOT the per-owner default resolver); return `{botId,status:'stopped'}`. `instantiate_bot` is side-effecting → flows through the EXISTING `dispatchSideEffecting` idempotency wrap (boundary_invocations four-tuple key + terminal replay — VERIFIED in dispatcher.ts + boundary.verification.integration.test.ts "same key → one bot"), so herobids threads the same `idempotencyKey` and Traderton dedups its own insert. NO dedup authored on the Traderton side.
          - **connectionId drop (decision 13):** Traderton bots have no connectionId. herobids keeps the local `connections` binding VALIDATION (platform authz) but passes only the resolved `venueAccountId` over the boundary.
          - **Seam (Shape 1, atomicity):** under herobids' EXISTING `pg_advisory_xact_lock(instantiate:userId:key)` → check local `blueprint_instantiation_requests` (replay/conflict AS TODAY) → call `instantiate_bot` FIRST → on success write the LOCAL platform records (`blueprint_instantiation_requests` idempotency row + `blueprint_usage_events`) in the same local tx → commit. On boundary failure: write nothing local, map to the POST /bots status codes (503/502/400/403/404). The agent branch (`createAgentFromPayload`, platform tables only) is UNAFFECTED.
          - **Residual divergence (Intentional, bounded — NO further escalation):** the source's single-tx atomicity splits (bot in Traderton DB, idempotency/usage in herobids DB). The only new failure mode = boundary success + local commit fail + client never retries → an orphaned STOPPED (inert: trades nothing, bills nothing) Traderton bot. Any retry converges to parity via the idempotency wrap + caller-supplied actorId. Log in 003 + 001; a reap-orphans reconciler is 010 backlog, NOT a cutover gate. VERIFY: Traderton `idempotencyRetentionHours` (default 168h) must exceed the client retry horizon (config check, not a new decision).
        - [x] **c4.9e — DONE (folded into c4.9a above).** provider-links.ts local `bots` reads removed; relies on the authoritative boundary `deprovision_venue_account` guard already in `deleteProviderLink`.
        - [ ] **c4.9f — THE DROP. ❌ PRECONDITION STILL NOT MET — blocked by c4.9i (2026-09-15, second false-green caught at drop time).** ⚠ ROOT CAUSE: every c4.9 "apps/**/src is clean" audit this session used the grep tool's brace/glob include pattern (`**/herobids/apps/**/src/**/*.ts`), which returns FALSE "No matches" in this environment. Re-audited with reliable ripgrep at drop time (Implementer + coordinator): **direct trading-table query access = ZERO (genuinely clean), 11 of 13 trading repo classes = ZERO importers (genuinely orphaned).** BUT TWO kept db symbols still have LIVE worker importers reading dropped tables: **`BotRepository` (3 files: index.ts, agent.ts, agent-message-broker.ts + the agent tools analytics/account/bots/resolvers/risk-limits via ctx.botRepo) reads bots/positions/fills/venue_accounts/analytics; `PgJournal` (4 files) reads journal_events.** These are the AGENT RUNTIME's OWN trading reads (get bots, open positions, analytics, journal) — never re-pointed over the boundary (the agent runtime lives in herobids; only decision EXECUTION crosses the boundary). index.ts:160 even labels BotRepository a "vestige read surface." Dropping bots/positions/fills/venue_accounts/journal_events now → worker `relation does not exist`. → NEW SLICE **c4.9i** (route to decision agent): re-point the agent-runtime BotRepository + PgJournal reads over the EXISTING boundary read tools (get_agent_positions, list_bots/get_bot_status, agent analytics, get_agent_journal_events — the c4.9b/D1-c1 seams), then remove the two symbols. c4.9f CANNOT proceed until c4.9i lands. **NOTE: ALL prior c4.9 "precondition met" / "zero access" claims are SUSPECT — re-audit every table with ripgrep, not the glob tool, before the drop.** NOT YET EXECUTED (destructive; human-gated go).
          [SUPERSEDED] ✅ PRECONDITION MET on the CORRECTED drop set (2026-09-15) [FALSE — glob-tool false-green missed BotRepository + PgJournal worker readers; see above]. History: the 2026-09-12 "precondition met" claim was incomplete — it enumerated only the 17 CORE tables and a `[historical, RETRACTED]` note had conflated `market_assessment_*` into a "FULL set." The 2026-09-15 holistic review caught ~20 live `market_assessment_*` worker sites and forced the scope question. **c4.9h RULING (decision agent, 2026-09-15): `market_assessment_requests`/`_runs`/`_artifacts` are ALL PLATFORM-KEEP** — agent preset-review/strategy-selection runtime (decisions 7–9) + platform orchestration/billing (decision 15); same class as wake-gate / PresetTransitionPort / assess_strategy_preset already ruled platform in 001. Traderton has ZERO runtime access to them (schema/types copied for the DEFERRED runtime-relocation future only; `requests` never copied); its own schema comment says "platform-owned assessment executions." Dropping them would delete LIVE platform behaviour (billing reservation/daily caps/dedup/preset-change) → they are **REMOVED from the trading-drop set**, NOT re-pointed, NOT a Gap. **The trading-drop set is therefore the 17 CORE tables ONLY:** bots, fills, positions, venue_accounts, user_credentials, journal_events, decisions, orders, execution_plans, decision_failures, llm_decision_artifacts, balance_snapshots, decision_contexts, instruments, token_safety_overrides, reconciliation_events (+ market_assessment_* EXCLUDED). **Audit re-confirmed clean (2026-09-15):** the 17 core tables (direct + repo-class) AND the residuals balance_snapshots/decision_contexts/token_safety_overrides = ZERO live herobids `apps/**/src` access (only comments / Buffer.from / type-only $inferSelect). herobids app source is clean of everything IN the corrected drop set → c4.9f UNBLOCKED, awaiting the destructive-drop go. After c4.9d-FG (instantiate write → boundary) + removing the last local `venue_accounts` binding read in blueprints.ts (boundary owner-guard is the authority now; 003 entry), a full grep audit (`.from/.insert/.update/.delete(<table>)` + raw `FROM/INTO/UPDATE/DELETE <table>`, ex-tests, across apps/api + apps/worker) finds only COMMENTS referencing removed queries — no live access to bots/fills/positions/venue_accounts/user_credentials/journal_events/decisions/orders/execution_plans/decision_failures/llm_decision_artifacts/balance_snapshots/decision_contexts/instruments/token_safety_overrides/reconciliation_events. Worker source CLEAN. Ready to drop the tables (migration) once committed + a final confirm. D2 greenfield NO-OP. Merge-to-`main` remains the ONE hard stop.
          ⚠ **BLOCKED by c4.9g (2026-09-12 audit): the herobids WORKER still accesses trading tables INDIRECTLY via repository classes.** The earlier "worker CLEAN" note was based on a DIRECT `.from(table)` grep, which missed repository-mediated access. `apps/worker/src` instantiates + uses 4 trading repos at runtime — see c4.9g below. c4.9f cannot drop until c4.9g lands.
          **[historical] ⚠ RE-SCOPED: the drop set is the FULL trading-table set, not 6.** Beyond bots/fills/journalEvents/positions/venue_accounts/user_credentials, the trading tables also include `decisions`/`orders`/`execution_plans`/`decision_failures`/`llm_decision_artifacts` (+ possibly balance_snapshots/decision_contexts/instruments/token_safety_overrides/market_assessment_* — needs a full herobids reader audit per table). Only after c4.9a–e + the NEW decisions/decision-failures re-point family land + a re-audit shows ZERO runtime query access to the FULL set in `apps/**/src`: drop the tables (migration). D2 greenfield NO-OP confirms empty. Mechanical migration; merge-to-`main` remains the ONE hard stop.
        - NOTE: also re-confirm the already-boundary-routed reads have no residual local table import used at runtime (bots.ts/billing.ts/admin.ts/views.ts/exports.ts/analytics.ts/reconciliation.ts/blueprint-performance-scorer/agent-interactivity done). Agent-evaluation worker path already boundary-sourced (get_agent_fills/journal/positions).
        - [x] **c4.9g — DONE (2026-09-12). herobids WORKER trading-repo usage REMOVED — the final c4.9f drop-blocker is cleared.** Per decision-agent per-usage rulings (001 ledger): g4 find_instrument RE-POINTED over the existing boundary tool (ctx.tradertonBoundary; identical shape; fail-closed, no local fallback); g1 decision-failure persistence REMOVED (Gap — matches Traderton's M1 no-op; in-memory circuit breaker + rejection-return preserved); g2 llm-artifact persistence REMOVED (Gap — Option 1 drop, agent reasoning untouched); g3 instrument-population DELETED (Deferred — Traderton must wire its own copied populateInstrumentsFromVenues at startup before cutover); g5 orphaned-position-cleanup DELETED (dead code; Traderton actor owns it). 9 files (2 deleted). herobids worker+api tsc clean; changed suites 24/0. CodeReviewer PASS (no CRITICAL/HIGH/MEDIUM; 1 deferred LOW = vestigial instrumentRepo? type-field on shared TradingToolContext, in Outstanding). **Grep-audit: herobids `apps/**/src` (api + worker, ex-tests) now has ZERO trading-table/repo runtime access — only a comment remains. c4.9f precondition MET.**
          [historical audit] **AUDIT DONE 2026-09-12.** `apps/worker/src` accesses trading tables INDIRECTLY through 4 repo classes — all PERSISTENCE/READ, NOT trading logic (the trade EXECUTION already goes over the Traderton boundary via the write-adapter `TradertonSideEffectBoundary`; the circuit-breaker is IN-MEMORY, not a `decision_failures` read). Classification:
          - **(g1) DecisionFailureRepository.insert** — `agents/agent-decision-handler.ts` `recordDecisionFailure`: fire-and-forget (`.catch()` logs) audit of a decision REJECTION/ERROR to `decision_failures`. NOTE: Traderton's own `submitDecision` (decision-intake.ts) DEFERRED `decisionFailureRepo.recordFailure` as an M1 no-op (event emits → item C2) — so Traderton does NOT currently persist these. Four-risk (write; parity question: author a boundary write vs drop-as-Gap given Traderton deferred it).
          - **(g2) LlmArtifactRepository.insert** — `agent.ts` `onArtifact` callback: fire-and-forget persist of the LLM decision artifact (prompt/response/parse-status/tokens/latency) to `llm_decision_artifacts`. Audit trail. Four-risk (write).
          - **(g3) InstrumentRepository.upsertInstruments** — `instrument-population.ts`: best-effort STARTUP population of `instruments` from venue market metadata (non-blocking; symbol validation continues via in-memory VenueInstrumentCache). Four-risk (write; is instrument population a Traderton-owned concern now?).
          - **(g4) InstrumentRepository.search** — `tools/find-instrument.ts` (the `find_instrument` agent tool) + `agent.ts` wiring: READ of `instruments`. Re-point over a boundary read (or Traderton owns find_instrument — Traderton HAS a `find_instrument` tool already, see traderton tools/find-instrument.ts).
          - **(g5) PositionRepository.upsert** — `reconciliation-orphaned-cleanup.ts`: WRITE a flat position to `positions` to auto-close orphaned shadow/paper positions during reconciliation. Four-risk (write; reconciliation is venue-account-scoped — does Traderton's reconciliation own this now?).
          Pivotal question (route to decision agent): Traderton is the successor authority AND already OWNS the decision/execution pipeline + has find_instrument + reconciliation. For each write, is the faithful move (a) author a boundary write tool so Traderton persists it, or (b) drop it as best-effort audit that greenfield-Traderton doesn't persist yet (record a Gap in 001, defer the tool to 010)? g4 (read) is a clean re-point/removal (Traderton find_instrument likely already the successor). Decompose per-repo after the ruling.
  - [~] **D1-cred — `user_credentials` split (C; NON-TRADING HALF DONE; 004 "Q3" + CORRECTION). NO BACKFILL (greenfield).** DONE: new herobids-local `platform_credentials` table + migration 0069; re-pointed gmail-credential-resolver, connections-oauth, setup.ts non-trading path, plan-guards.checkCredentialLimit. Trading paths FROZEN. api+worker tsc clean; 63/0; reviewed (no CRITICAL/HIGH). REMAINING: the trading half (credentials.ts + accounts/connections credentialId validation + `user_credentials` drop) is D1-cred-Q4 + D1-c4. CORRECTED scope (004 "D1-cred scope CORRECTION"): `credentials.ts` is a TRADING path (validates venue secrets) — NOT moved to platform_credentials. **Non-trading half (proceed now, settled):** new herobids-local `platform_credentials` table + re-point `gmail-credential-resolver`, `connections-oauth`, and the setup.ts NON-trading path; re-point `plan-guards.checkCredentialLimit` to count `platform_credentials` only. No backfill (greenfield). **Trading paths FROZEN** (`credentials.ts` + accounts.ts/connections.ts credentialId validation) — belong to the D1-c4 trading-drop / deprecation workstream; leave `user_credentials` intact until then (credentials.ts keeps writing it — no user-facing regression). herobids schema migration (branch-only).
  - [ ] **D1-cred-Q4 — RESOLVED (human: REMOVE ENTIRELY). Folded into D1-c4.** Remove the standalone `/credentials` flow entirely: herobids API `credentials.ts` (route + registration + tests + schemas + credential audit events + credential-dependents if only used here) + the `credentialId`-validation branches in accounts.ts/connections.ts; herobids WEB CredentialsPage + `/credentials` route + sidebar + api-client `credentials` + i18n `nav.credentials`/`credentials.*` + AgentCapabilityPage link (relocate `PROVIDER_TEMPLATES` which ProviderSetupForm imports). Then `user_credentials` has no writer/reader → drops at D1-c4 (greenfield, empty). See 004 "D1-cred-Q4 — RESOLVED".
  - [x] **D1-b (candle-evidence re-point; GROUP 1; DONE — Option B derived-evidence hybrid, human-approved).** traderton `score_candidate` returns derived `candleWindow` + `get_volatility` returns full `VolatilityEvidence` (verbatim `computeVolatilityEvidence` copied into `@traderton/market-data`); herobids PlatformAssessor sources volatility + window/count + regime as DERIVED evidence over the SYSTEM read boundary (no raw candles). Removed the last in-process market-data authority from the worker: `createProviderRegistry`/`sharedMarketDataRegistry`/`scannerCandleFetcher` + `scanner-candle-fetcher.ts` deleted; `@herobids/{market-data,venues}` value imports gone from the worker. `symbolCandles` = availability flag. Two review passes: 2 HIGH fixed (averageTrueRange unit break; authored regime thresholds → both fixed by copying the derivation verbatim Traderton-side), MEDIUM (Zod boundary validation) applied. traderton 82/0; herobids MI 317/0. Commits: traderton `get_volatility`+indicators; herobids evidence re-point.
  - [x] **D1-3b (trading key-gen; GROUP 3b; DONE — settled by parity, 004 "Q1").** `provision_venue_account` gained a GENERATE mode (Traderton mints the venue keypair behind the boundary via the copied `generateWallet`, returns only the public `{address,network}`, never the private key). herobids generated-trading path re-points to request generate mode; `generateWallet` FULLY removed from the herobids API (no non-trading generated path — schema forces generated⇒trading); `deriveSolanaAddress` kept for manual Jupiter. Custody bar met (reviewed). traderton provisioning+boundary 25/25; herobids API tsc clean + setup 30/0. 005 updated with the generate-mode contract. The last `@herobids/venues` key-gen value import is gone from the API.
  - **GROUP 3a (`.probe()` in API): KEEP** — platform-permissible (API process, unauthenticated venue-metadata preview), delete-at-cleanup per §2 (non-blocking; NOT a D1 removal).
  - **D1 sub-obligation (from A1 review, MEDIUM):** when the DELETE pre-guard + local `bots` mirror are removed at D1-a, the route's `delete_bot` 409 BACKSTOP (`details.errorCode==='bot.running'` → 409) becomes the SOLE 409 defense. It is untested today (the pre-guard fires first; a divergent stub would test an impossible-today state). ADD a functional test isolating the backstop (read reports stopped / delete returns bot.running → assert 409) AS PART OF D1-a, when the pre-guard is removed.
- [ ] **D2. Pre-existing row migration → NO-OP (greenfield; 004 "no real data, starting afresh").** There is NO real data — no pre-cutover trading-credential/venue-account/bot/fill/journal rows to migrate. Reduces to "confirm the old tables are empty, then drop" (folds into the D1-c4 / D1-cred drops). The `Deferred (required for cutover)` obligation is satisfied by the absence of data, NOT a migration. No data-seeding anywhere in D3/D4.
- [ ] **D3. herobids functional/E2E boundary-aware + true cross-stack E2E** (herobids stack + live Traderton boundary, matching signing creds). Gate criterion #3. Some sub-parts 4-risk.
- [ ] **D4. Operational-readiness + rollback proof (007) + staging soak** → then the HUMAN MERGE APPROVAL (the hard stop).

## §2 — Optional / NON-blocking (NOT gate obligations)
- Deprecated `POST /venue-accounts` preview endpoint (delete at cleanup; admin-only; no scripts POST to it).
- Dev `CREDENTIAL_ENCRYPTION_KEY` in committed compose (convenience).
- Code-review LOW/MEDIUM follow-ons (swap M1/M2 dedupe+tie-break test; bin.ts cast comment; B2 DRY helper; regime magic-literal comment).
- 010 backlog B1–B10 (later-options). Traderton-native metering / bot cloning (Deferred-optional).

## §3 — Undetermined (resolve at slice time)
- ~~**C2 blocking-vs-optional:** confirm swap evidence path still returns null candles (inert)~~ RESOLVED: the swap `score_candidate` build ALREADY LANDED (token→pool→candles→score behind the boundary; strategy.ts + 7 swap tests + boundary-e2e). The "inert/deferred" note was stale — C2 is DONE, not optional-because-inert.
- **`generateWallet` (from @herobids/venues) in api routes** (setup.ts/chat.ts/index.ts/accounts.ts): trading-owned (must move) or platform-side key-gen herobids keeps? Route classification via 008 if trading-adjacent.

## Outstanding Issues (non-blocking review findings; MEDIUM/LOW only — no CRITICAL/HIGH)
Accumulated from per-slice CodeReviewer passes. Grouped by item. None gate the merge.

- **[B2]** MEDIUM — boundary success payload is `data: unknown` (`TradertonReadResult.success`); success-shape parity (`priceUsd`/`source`/`fetchedAt`/`stale`) is guaranteed only by the external Traderton `get_price` tool, invisible to the herobids type system. Inherent to the transitional boundary design (same as all read re-points, incl. `get_risk_limits`). Mitigation lives at D3 (cross-stack E2E pins the real wire shapes). Not a code defect.
- **[B2]** LOW — no per-tool test for boundary `transport_error` / `in_progress` on `get_price` (mapper handles both and is separately tested). Optional: add a `transport_error → { retryable:true, fault:true, errorCode:'boundary.transport_error' }` case to fully pin the tool's unhappy-infra contract.
- **[B2]** LOW — `fault` intentionally absent on the pre-existing in-process `price_service_not_configured` branch while other branches set it explicitly. Pre-existing, harmless minor inconsistency.
- **[B3]** ~~TEMPORARY GAP — watch-threshold wakes OFF until B3-monitor~~ **CLOSED**: B3-monitor landed — watch-threshold wakes are re-enabled, sourced from boundary `check_watches` (evaluation authority = Traderton), with the platform push/dedupe/rate-limit/emit machinery kept in the herobids monitor and reset-dedupe parity preserved via `check_watches` `reset[]`.
- **[B3]** MEDIUM (no-boundary config) — with the boundary present (the cutover config) isolation holds and the summary is boundary-sourced. In a NO-boundary deployment, disabling the `watchThresholds` family means the `agent:watches:summary:*` cache is no longer written by the monitor (summary now rebuilt per-tick) and watch wakes don't fire. Acceptable for the cutover target (boundary is present); revisit if no-boundary must stay a supported runtime.
- **[B3]** LOW — `parseRuntimeActiveWatch` (in `agent-watch-view.ts`) is exported but covered only indirectly via `parseBoundaryWatchList`; add a direct test or drop the export.
- **[B3]** LOW — `loadActiveWatches` summary error-path fallback may re-fetch `list_watches` on the (rare) error branch; by design, noted for awareness.
- **[B4]** LOW — the `market_data_not_configured` guard block is now duplicated across the 5 tools alongside the boundary guard; pre-existing + transitional (in-process arm slated for deletion at D1), abstracting now would be premature.
- **[B4]** LOW — no per-tool `transport_error`/`in_progress` mapping test for a market-data tool (the mapper is shared + separately tested; one `failure` case is covered here). Optional parity coverage.
- **[B4]** LOW — `market-data.test.ts` missing trailing newline (trivial nit).
- **[B5]** LOW — two ATR computations now coexist in traderton `indicators.ts` (the Wilder-smoothed ATR inside `adx`, and the new simple-mean `calculateAtrPercent`). Intentional: copy-fidelity to the herobids source requires the standalone simple-mean version; not reused from `adx`.
- **[B5]** LOW — `get_volatility` rate_limit branch not unit-tested (parity with check_regime, whose test only covers symbol normalization). Optional add.
- **[B5]** LOW — `get_volatility` exposes a `symbol` param but agent.ts always calls with `payload:{}` (defaults BTC, matching source). Harmless LLM-path surface, consistent with check_regime's benchmarkSymbol.
- **[B6][OPS PREREQ — not a Gap/degrade]** The traderton BOUNDARY process must have `SCRAPFLY_API_KEY` in its env for economic-calendar acquisition to run (identical to the herobids worker requirement today; absent → same warn + empty macro block degrade in both). Additionally, the fallback calendar parser's LLM config is sourced from env (`LLM_BASE_URL`/`LLM_MODEL`/`LLM_API_KEY`/`LLM_TIMEOUT_MS`) since the trading-only Traderton AppConfigSchema drops the platform `appConfig.llm` block — the DOM parser is primary, LLM only activates if ForexFactory markup changes, so these are optional in the common case. Record for the boundary deployment/ops-readiness (007).
- **[B6]** MEDIUM→settled-as-LOW — the relocated bin.ts acquisition loop uses `console.info/warn/error` (with eslint-disable) rather than a structured logger. bin.ts has NO logger in scope (its only other logging is the existing `main().catch` console.error) — console is the file's established convention for this composition root; introducing a pino logger is out of B6 scope. Left consistent with the file.
- **[B6]** LOW — bin.ts LLM model default literal `'anthropic/claude-sonnet-4-5'` is a magic literal (env fallback); acceptable given dropped platform config.
- **[B6]** LOW — herobids failure log now `{ kind: result.kind }` instead of the old `{ error }` (correct for the boundary transport shape; slightly less diagnostic — could add the boundary `code` on `kind==='failure'`).
- **[B7][ACKNOWLEDGED 2026-09-12 — CLOSED]** The fail-fast startup guard changes behaviour in the misconfigured no-boundary+trading case (previously silent in-process fallback → now a hard boot error). Accepted Intentional-divergence per the B7 decision (boundary is mandatory at cutover). Human acknowledged; no ratify-list entry remains.
- **[B7]** LOW — two residual vacuous `if (tradertonReadBoundary)` conditionals remain in `refreshVenueIntelligence` (perps + DEX-discovery blocks) after the early-return makes the boundary always truthy; correct behaviour but reads as if a no-boundary branch still exists. The DEX-search block was unwrapped; the other two could be too for consistency (stylistic).
- **[B7]** LOW — the fail-fast startup guard has no direct unit test (it's a 2-line boolean: `deriveHasTradingCapability(skills) && !boundary → throw`). Low risk; could extract the predicate for a unit test.
- **[B7]** LOW — get_price no-boundary degrade string standardized `price_service_not_configured` → `market_data_not_configured` (only reachable in tests / hidden-tool path; consistency with the other market-data tools).
- **[Wave-B batch review]** CLOSED — the final gap-review found 2 MEDIUM residual couplings (dead-but-compiled trading market-data code in the agent bundle); both fixed: (1) `tools/market-data.ts` `CANDLE_PROVIDERS` value import replaced with a verbatim-inlined `REGIME_SYMBOL_FORMAT_HINT` string constant (rendered description byte-identical); (2) dead `intelligence-tools.ts` + its test DELETED (no production importer). Agent container now has ZERO `@herobids/market-data` value imports (only a type-only `RegimeParams` in agent.ts, erased at compile). Isolation objective fully met for the agent container (modulo the logged B3-monitor watch-wake deferral). Worker suite 3075/0.
- **[VERIFY-PROCESS]** IMPORTANT — the herobids ROOT `pnpm lint` (`tsc --noEmit` with project references + `files:[]`) does NOT type-check `apps/worker` in --noEmit mode (reports false-green). Worker-touching slices MUST verify with `pnpm exec tsc --noEmit -p apps/worker/tsconfig.json`. (Discovered during B5; earlier worker slices B2–B4 re-verified fine, but always use the direct worker tsc going forward.) Traderton worker resolves the BUILT `@traderton/market-data` — rebuild the market-data package after changing its exports.

- **[D1-a / GROUP 2]** LOW (informational; out of scope) — orphaned `streams.public` config subtree. After removing the dead public-stream pool, `apps/worker/src/index.ts` was the sole worker consumer of `appConfig.streams.public`; it is now dead config in the worker path (the `streams:` block still exists in `packages/domain/src/config/schema.ts` ~L1708 and `config/default.yaml` ~L152). `streams.private` may still be consumed elsewhere — only the `public` subtree is implicated. Config-field cleanup was explicitly scoped out of this slice; note for a future cleanup pass. Not a blocker.

- **[D1-b]** LOW — `get_volatility` iterates the candle set twice (`calculateAtrPercent` + `computeVolatilityEvidence`), each independently. Negligible at these sizes; could share one TR pass. Noted only.
- **[D1-b]** LOW (accepted transitional) — the candle adapter calls BOTH `get_volatility` and `score_candidate` per assessment, and `generateScorecards` calls `score_candidate` again per-preset. Extra behind-boundary fetches vs the old single in-process fetch. Accepted transitional cost (M1); revisit if assessment latency matters.
- **[D1-b]** LOW — `evidence-adapters.ts` candlesAdapter passes `providerSymbol` as both `symbol` and `instrumentId` in the `score_candidate` payload; fine for orderbook (the only supported kind), minor readability nit.

- **[D1-3b]** MEDIUM→LOW — the worker provisioning test's `expect(envelope).not.toContain('secret')` is a brittle literal-word check (passes coincidentally); the real custody guard is the value-based `not.toContain(decryptedSecret)` assertion (present + correct). Tighten the literal check or drop it.
- **[D1-3b]** LOW — `generatedWalletAddress`/`generatedNetwork` are parallel nullables always co-set; a single `{address,network}|null` would make the invariant structural.
- **[D1-3b]** LOW — `generate.network` is an unconstrained caller-supplied free-string not cross-validated against the venue; intentional per the slice (pre-boundary caller's concern), noted as an explicit assumption.
- **[D1-cred]** LOW — `resolveGmailTokens` has no DIRECT unit test (exercised only via a `vi.mock` in `email.test.ts`); the real DB join/re-encrypt against `platform_credentials` is tsc-guarded but not runtime-asserted. Future: an integration test seeding platform_credentials+connections+agent_connections. Pre-existing gap, not introduced by the split.

- **[D1-c1]** MEDIUM — the inlined `loadAgentBotIds` helper is triplicated across FillRepository/PositionRepository/PgJournal (byte-identical). Acceptable per the sub-step (small, copy-faithful, avoids a cross-class dep); collapse to one shared db-internal `agentBotIds(db, agentId)` free function if a later sub-step introduces a shared module boundary.
- **[D1-c1] DONE (all 6 sub-steps).** (1) loader repo methods in @traderton/db (copied verbatim); (2) three agent-scoped boundary tools get_agent_fills/journal_events/positions (thin seams, scope-guarded); (3) registered via botManagementTools + documented in 005; (4) herobids evaluation evidence-assembler re-pointed over a per-agent SYSTEM-subject read boundary (date-rehydration mappers; sessions/metadata/costs local; fail-closed; core-must-succeed); (5) herobids API agent exports (trades/journal/costs/bundle) re-pointed over a per-request USER-subject boundary (api-local seam; boundary→HTTP mapping; sessions/config/bot/account untouched). Final verify: both repos tsc clean; traderton 118/0; herobids MI+exports 190/0. sessions correctly stays LOCAL (platform table). Reviewed each sub-step (no CRITICAL/HIGH). The fills/journal/positions LOCAL reads now only remain in the untouched bot/account export endpoints (a later slice) + the ruling-5 route fallbacks (D1-c4).
- **[D1-c1]** LOW (row-shape contract) — export/evaluation parity rests on the boundary tool payload row shape equaling the herobids DB row shape. VERIFIED for fills (both schemas have realizedPnlDelta — no additive divergence); the tools return Traderton `$inferSelect` rows which match herobids columns. A cross-boundary contract test pinning this belongs to L3e; noted so a future schema drift is caught.
- **[D1-c1]** LOW — the loader unit tests are mock-db based (can't execute SQL WHERE), so the from/to time-filter test only proves the union composition survives the opts path (not that gte/lte apply); the `at`-snapshot test IS meaningful (real JS post-filter). Real WHERE/arm-content correctness deferred to integration/L3e. Consider distinct per-arm row IDs so an arm swap would be caught.

## Hard stops (008 §8.2) — the loop halts ONLY for: merge-to-main; a decision-agent-escalated product/policy call; an on-branch-unresolvable blocker; backlog exhausted.

- **[c4.1]** MEDIUM — `capabilities/trading.ts /outcomes` `feesByCurrency` string shape: in-app `String(numberSum)` yields `'7.4'` where the old DB `sum(numeric fee)` preserved scale `'7.40'`. Matches the accepted exports.ts seam precedent; `feesByCurrency` from this endpoint is NOT consumed by the web app (api-client `TradingOutcome` has no such field). Sibling `bots.ts` local path still emits `'7.40'` → minor cross-endpoint inconsistency. Accept unless a fixed-scale consumer emerges.
- **[c4.1]** MEDIUM — `totalPnl` summed as IEEE-754 double before `.toFixed(6)` (vs DB exact-numeric sum). Theoretical 1-ULP last-digit gap on pathological decimal sets; matches exports.ts precedent; unobservable at realistic PnL magnitudes.
- **[c4.1]** LOW — `activity` in-app stable-sort tie-break on equal timestamps depends on boundary array order; benign (old `ORDER BY LIMIT` tie order was already unspecified).
- **[c4.1]** LOW — only `state` + `/trades` have explicit "503 when boundary unconfigured" tests; `activity`/`outcomes`/`positions` share the same `agentReadBoundary` helper (posture is covered) but lack per-endpoint 503 assertions. Optional hardening.
- **[c4.1]** (FIXED in-slice) LOW — positions endpoint no longer fetches fills when positions is empty (early-return reordered before the fills fetch).

- **[c4.2-tools]** LOW — the ISO-date-parse tests for `get_owner_bot_fills`/`get_owner_fills` assert only that the query builder ran (not the parsed Date bounds reached the WHERE) — the inline `and(...conditions)` makes the predicate awkward to capture. Shallow but non-blocking.
- **[c4.2-tools]** LOW — `get_owner_bot_fills`/`get_owner_fills` have no explicit ORDER BY (parity-faithful: the herobids source export routes also don't order). Future consumers must not assume stable ordering.
- **[c4.2-tools]** LOW — 006/001 per-tool scoring entries for the 5 new tools are a doc follow-up (005 done). Not functional.

- **[c4.2-exports]** LOW — the `details.errorCode` unwrap is now duplicated verbatim across 3 sites (bots.ts readBoundary, exports-traderton.ts, worker read-adapter). Could extract a shared `unwrapToolErrorCode` helper; deferred per copy-parity + KISS.
- **[c4.2-exports]** LOW — `loadBotStatus`/`loadBoundaryObject` cast to Record<string,unknown> after an object-guard (consistent with existing seam style; TS-strict-clean).

- **[c4.2-recon]** MEDIUM (parity delta, accepted) — `/bots/:id/reconciliation-events` error ordering flipped: old = ownership(404)→validation(400); new = validation(400)→boundary-ownership(404). For a request that is BOTH unowned AND has a bad query, old returned 404, new returns 400. Both are 4xx client errors, no data leak; 400-first is arguably the better posture. Noted in 003-anomalies. Not a regression to fix.
- **[c4.2-recon]** LOW — `reconciliationRoutes` `_db` param now unused (kept for signature stability; drop at c4.9). Tool `since` is `z.string()` not `.datetime()` (endpoint validates first, so protected; harden if ever called directly).

- **[c4.4 finding — connections.ts additional trading reads]** GROUNDED 2026-09-12: `apps/api/src/routes/connections.ts` `selectConnectionView` has MULTIPLE trading-table subquery reads beyond credentials: `referencingBotCount` (subquery on `bots`), `venueAccountLabel`/`venueAccountVenue`/`venueAccountRef` (subqueries on `venue_accounts`), plus `credentialLabel`/`credentialProvider` (subqueries on `user_credentials`). c4.3-api removes ONLY the `user_credentials` credential subqueries. The `bots` + `venue_accounts` display subqueries are ADDITIONAL c4.4 drop-blockers (connections is a KEEP platform table but reads trading tables for display) — must re-point/remove before c4.9. Added to the c4.4 reader set (the original audit under-counted connections.ts).

- **[c4.3-api]** DONE detail: deleted routes/credentials.ts (+test) + credential-dependents.ts + index.ts registration + CreateCredentialSchema/RotateCredentialSchema from schemas.ts + the user_credentials credential DISPLAY subqueries in connections.ts selectConnectionView. RELOCATED `canonicalizeVenueSecrets`/`validateVenueSecrets`/`SecretValidationError` VERBATIM from credentials.ts → new `apps/api/src/providers/venue-secrets.ts` (they're provider-secret validation utilities consumed by the surviving setup.ts, NOT the CRUD route); setup.ts re-pointed. Removed credentialRoutes from functional helpers + the createCredential helper/assertions from capability-model.functional.test.ts. FIXED in-slice (CodeReviewer M1): removed 2 stale connections.test.ts tests asserting credentialLabel/credentialProvider (false-confidence — mock returned them; the real query no longer emits them) + pruned them from CONNECTION_ROW + allowedKeys. api tsc clean; connections 34/0; full api routes+services 1223/0. CodeReviewer PASS (no CRITICAL/HIGH; M1 FIXED).
- **[c4.3-web] MUST-HANDLE (from c4.3-api CodeReviewer LOW):** web still consumes the deleted `/credentials` endpoint (api-client.ts `credentials` object) + declares `credentialLabel`/`credentialProvider` NON-optional on the Connection type (api-client.ts ~1412) + renders them in ConnectionsPage.tsx ~349. The API now OMITS credentialLabel/credentialProvider from `/connections` → they'll be `undefined` at runtime. c4.3-web must: remove the `credentials` api-client object, drop credentialLabel/credentialProvider from the Connection type + ConnectionsPage rendering, in addition to removing CredentialsPage/route/nav/i18n + relocating PROVIDER_TEMPLATES.

- **[c4.4 INPUT — venue-accounts API may now be DEAD after c4.3-web]** With VenueAccountsPage removed (c4.3-web) + the `venueAccounts` web api-client object deleted, the web no longer calls `POST /venue-accounts` (local venueAccounts WRITE), `GET /venue-accounts` (list), or `DELETE /venue-accounts/:id`. AUDIT at c4.4: confirm NO remaining live caller (web or elsewhere) of `venueAccountRoutes` endpoints. IF dead → c4.4 can REMOVE the whole `accounts.ts` venueAccountRoutes (POST local write + GET list + DELETE) rather than re-point the POST — simpler AND fully isolation-compliant (no local venueAccounts write remains). This also resolves the deferred c4.3 `accounts.ts` userCredentials-validation branch (it dies with the removed POST). The DELETE endpoint is already boundary-re-pointed (deprovision_venue_account) — if any admin/other caller needs venue-account delete, keep just that one re-pointed. Verify callers before removing.
- **[c4.9c/d/g]** LOW — `/agents/outcomes` PnL now sums `Number(realizedPnl)` in JS then `.toFixed(6)` (vs the old SQL `sum()`); last-decimal drift is possible on very large position sets. Consistent with the migration-wide in-JS-fold pattern (dashboard/analytics) — not a new regression. Harden only if exact SQL-decimal parity is later required.
- **[c4.9c/d/g]** LOW — asymmetric decision-failures degrade: telegram `/log` degrades to platform-only (never 503) when the `get_agent_decision_failures` boundary read fails, whereas the HTTP `/agents/:id/decision-failures` route fails closed with 503 for the same tool. Intentional (never 503 a chat command) and covered by handleLog tests; documented here for the ledger.
- **[c4.9d-FG]** LOW — herobids blueprints.ts instantiate seam: on boundary success `persistedBotId = successPayload.botId ?? actorId`; since instantiate_bot echoes the caller-supplied actorId as botId, the `?? actorId` fallback is effectively dead (defensive). Optional: assert/log if `botId !== actorId` to catch a boundary contract drift early.
- **[c4.9d-FG]** LOW — the instantiate_bot WRITE reuses the read-timeout knob (`deadlineMs: tradertonReadTimeoutMs ?? 30_000`), mirroring bots.ts (consistent). Note only if a write-specific deadline is ever wanted.
- **[c4.9g]** LOW — the optional `instrumentRepo?` type-field lingers on the shared herobids domain `TradingToolContext` interface (packages/domain) + its stub in watch.test.ts, after g4 removed the only runtime consumer. Type-only, zero runtime use. Left to avoid editing the shared contract beyond the worker slice; trivial follow-up cleanup.
- **[c4.9g / Traderton-side follow-ups required before/at cutover]** (a) **g3 Deferred-required-for-cutover:** Traderton must wire its copied `populateInstrumentsFromVenues` into `create-trading-runtime` worker startup, else `find_instrument` reads an empty `instruments` table. (b) **g1/g2 Gaps (sign-off):** no side persists decision-failures or llm-artifacts until Traderton wires those writes (deferred; get_agent_decision_failures returns empty in the interim). Greenfield → no history lost.

### Holistic cross-slice review (2026-09-15) — findings on the c4.9c→c4.9g arc
- **CRITICAL (RESOLVED):** the c4.9f "precondition met" claim omitted `market_assessment_*` (had ~20 live worker sites). Resolved by the c4.9h ruling above: market_assessment_* = PLATFORM-KEEP, removed from the drop set; drop set corrected to the 17 core tables; residuals (balance_snapshots/decision_contexts/token_safety_overrides) re-audited clean. Not a code defect — a scope/docs correction.
- **[c4.9d-FG] HIGH (to fix before relying on the concurrency claim):** the instantiate Shape-1 seam's SECOND local tx (writes blueprint_instantiation_requests + blueprint_usage_events after the boundary success) re-takes NO advisory lock and re-runs NO replay check. A genuinely-concurrent same-key pair that both cleared the first-tx replay check will both attempt `insert(blueprint_instantiation_requests)` → the `uq_bpir_user_key` unique index rejects the loser with an uncaught 23505 → 500 (not a double-create; the bot is single+inert). docs/003's "concurrent duplicates collapse" is stronger than the code. FIX: wrap the second tx in the same advisory lock + re-check/replay the idempotency row, OR catch the 23505 and convert to the 200 replay. Add a true-concurrency test (the existing "concurrent" test is serialized by the lock and only hits the local replay path).
- **[c4.9g] MEDIUM (cutover sequencing, already tracked):** g3/g4 — find_instrument now fails closed against Traderton's `instruments`; Traderton MUST wire its copied `populateInstrumentsFromVenues` at worker startup before the herobids table drop / cutover, else every instrument lookup fails closed. (Same as the g3 Deferred-required note.)
- **LOW:** vestigial `TradingToolContext.instrumentRepo?` type-field (already tracked).
- **Confirmed sound by the review (not rubber-stamped):** 17-core-table isolation; UUIDv5 actorId + crash-retry/different-body convergence; g1 removal preserves rejection-return + in-memory breaker; find_instrument boundary seam wired; instantiate_bot owner-guard subsumes the removed local venue_accounts check; connectionId→creatorId rescope applied uniformly + honestly recorded.

### c4.9i settled-portion — DONE (2026-09-15). Outstanding LOWs + the remaining blocker.
Implemented the settled c4.9i rulings (herobids consume-traderton): re-pointed the agent-runtime's live trading reads over existing boundary tools (resolvers→list_bots, risk-limits fallback, agent.ts:2696/3099 in-runtime open-position reads→get_agent_positions), removed dead botRepo fallbacks + the dead BOT_QUERY broker path + the dead bot-state-write mirror (toolBotRepo facade + ctx.botRepo collapsed), removed the redundant getResolvedVenueAccount (reads the KEEP connections descriptor field), Gap-dropped the PgJournal.append agent-activity writes. Worker+api tsc clean; affected suites 150/0; full worker suite 3036/0 (Implementer). CodeReviewer MERGE (no CRITICAL/HIGH/MEDIUM; verified the get_agent_positions union parity, the risk-limits boundary-absent fallback, and the approval-path descriptor equivalence). ripgrep confirms ZERO botRepo read calls (only the manage_bot KEEP path) + ZERO journal.append writes remain in the worker.
- **[c4.9i] LOW** — resolvers.ts extractBoundaryBots matches top-level `bot.symbol` from list_bots (was config.symbol); parity holds because list_bots flattens config.symbol→top-level. Add a comment noting the contract dependency.
- **[c4.9i] LOW** — agent.ts fetchAgentOpenPositions drops non-string-symbol rows (schema is NOT NULL; defensive). Optional debug-count on skip.
- **[c4.9i] LOW** — risk-limits buildRuntime openPositionsBlocked is always false on the degraded boundary-absent fallback; add a clarifying comment.
- **[x] c4.9j — DONE (2026-09-15). AlertDispatcher journal_events poll re-pointed over the boundary — the LAST herobids `journal_events` reader is removed, UNBLOCKING c4.9f.** RESOLVED as a polled cross-owner read (NOT a streaming boundary, NOT a Gap-drop). TRADERTON: authored 3 thin `read-database` pass-through tools in `packages/worker/src/tools/bots.ts` — `scan_trade_events` (cursor `{createdAt ISO,seenIds}`→Date-parsed / `typePrefixes` / `limit`) / `get_events_by_ids` (`{ids}`) / `get_event_by_id` (`{id}`→`event|null`), each `new PgJournal(ctx.db).scanAfter/getByIds/getById` reproduced VERBATIM, NO owner/actor filter (deliberate cross-owner — scanAfter is already cross-owner; the consumer's `typePrefixes` is the only filter), registered in `botManagementTools`. HEROBIDS: introduced a narrow `TradeEventFeed` port (`alerting/trade-event-feed.ts`), swapped `AlertDispatcher`'s constructor `PgJournal`→`TradeEventFeed` (body mechanical `this.journal.X`→`this.feed.X`; KEEP loop/lease/cursor/cooldown/delivery/policy untouched), authored a boundary-backed adapter (`alerting/boundary-trade-event-feed.ts`; mirrors `createTradertonReadBoundary`; Date→ISO outbound cursor + ISO→Date inbound `createdAt` rehydration mirroring `evidence-row-mappers` `toJournalRow`; THROWS on failure/transport_error/in_progress so the tick's catch reschedules without falsely advancing the cursor — NOT `return []`), wired a system-subject read boundary in `index.ts` (`actor {type:'system',id:'alert-dispatcher'}`, distinct from `market-intel`; fenced by consumer `allowedActorTypes:['system']` — no per-tool allow-list exists, OQ-2 accepted actor-type-only), OQ-4 degrade = symmetric config-guard: dispatcher not constructed/started when the feed boundary is unconfigured (mirrors the existing Telegram warn-and-return + `systemReadBoundary` undefined-when-absent), removed `const journal = new PgJournal(db)` + the now-unused `PgJournal` import (tsc `noUnusedLocals`-gated). Re-pointed AlertDispatcher tests onto a stub `TradeEventFeed` (removes the `as unknown as PgJournal` cast) + added a B3 adapter mapping test. traderton worker tsc + db/worker/boundary builds clean, bots suite 148/0 (+13); herobids worker tsc clean, alert-dispatcher + adapter suites 23/0. Ripgrep (`rg -n "PgJournal|journal_events" apps -g '!**/*.test.ts'`) confirms ZERO local `PgJournal`/`journal_events` READERS remain (only the boundary tool name `get_agent_journal_events`, comments, and the new port doc). Added to 005. Parity: preserved (re-point) + cross-owner exposure Intentional (actor-type-fenced). **← removes the last `journal_events` reader → c4.9f journal_events-drop precondition MET.**
- **⚠ RESOLVED c4.9f BLOCKER (was: AlertDispatcher journal_events poll):** superseded by c4.9j above — the AlertDispatcher now reads Traderton trade events over the boundary via `scan_trade_events`/`get_events_by_ids`/`get_event_by_id`; `journal_events` has no remaining herobids reader.

### c4.9j — trade-event feed + AlertDispatcher re-point — DONE (2026-09-15). Outstanding LOWs.
Authored 3 cross-owner read-database boundary tools (scan_trade_events/get_events_by_ids/get_event_by_id — faithful PgJournal.scanAfter/getByIds/getById pass-throughs, system-subject-scoped) + re-pointed the herobids AlertDispatcher onto a TradeEventFeed port/boundary adapter (throws on non-success so the cursor never falsely advances; Date↔ISO round-trip). Removed the last `new PgJournal(db)` reader in herobids. traderton worker tsc+builds clean, bots tools 148/0; herobids worker tsc clean, alert suites 23/0. CodeReviewer APPROVE (no CRITICAL/HIGH/MEDIUM; verified failure/cursor semantics + ISO↔Date + zero-reader). **This removes the LAST journal_events reader → the c4.9f drop precondition is now MET for ALL 17 core tables.**
- **[c4.9j] LOW** — boundary-trade-event-feed toRow() narrows id/type via `as string` (createdAt IS validated); consistent with the evidence-row-mappers precedent; a Zod row-parse would harden it.
- **[c4.9j] LOW** — traderton tool descriptions say "system-subject only" but enforcement is actor-type fencing at boundary config (aspirational vs mechanism; cosmetic).

### c4.9f — THE DROP — DONE (2026-09-15). The 16 core trading tables dropped from herobids.
After c4.9i + c4.9j cleared the last readers (ripgrep-verified, NOT the false-greening glob tool), dropped the 16 core trading tables from the herobids DB: bots, fills, positions, venue_accounts, user_credentials, journal_events, decisions, orders, execution_plans, decision_failures, llm_decision_artifacts, balance_snapshots, decision_contexts, instruments, token_safety_overrides, reconciliation_events. (The "17" in earlier notes was a persistent miscount — the canonical core set is 16.) market_assessment_* (c4.9h PLATFORM-KEEP), connections, agents, billing*, blueprints*, skills* all UNTOUCHED. The engine package (structural FillRepo/OrderRepo interfaces) untouched.
- **Migration:** `packages/db/drizzle/0070_drop_trading_tables.sql` — hand-written CUSTOM migration (drizzle-kit's interactive rename/delete resolver can't run headless), 16 `DROP TABLE IF EXISTS <t> CASCADE` (CASCADE = FK-safe ordering; IF EXISTS = greenfield-safe under reset-and-run's volume-wipe + replay). _journal.json + 0070_snapshot.json updated.
- **Code:** 16 schema files deleted + barrel exports removed; 5 orphaned trading-repo files + journal-pg.ts deleted; repositories.ts reduced (6 trading repos removed; BotRepository cut to only isConnectionOwnedBy, which reads the KEEP connections table); dead agent-evidence-loaders functions gutted (zero callers — superseded by the boundary agent-evidence path at D1-c1); 3 files' row types (FillRow/JournalRow/PositionRow) re-declared as explicit interfaces faithfully reproducing the dropped schema columns (source of truth = Traderton's identical schema).
- **Verified:** @herobids/db build clean; apps/api + apps/worker tsc clean; db package suite 138/0. CASCADE-safety enumerated: NO KEEP table FKs into a dropped table. CodeReviewer: COMMIT after fixing 1 HIGH (orphaned bots.test.ts doing readFileSync on the deleted bots.ts → ENOENT at vitest collection; DELETED — its unique-index regression guard is moot now the table is gone). MEDIUM: migration comment 17→16 (fixed); dual row-type definitions (exports-traderton.ts + evidence-row-mappers.ts) can drift — accepted isolation-boundary tradeoff, follow-up = shared module + boundary contract test.
- **Reversibility (confirmed earlier):** branch-only + reset-and-run wipes the DB volume + replays migrations, so reverting = checkout main (no 0070) + reset-and-run rebuilds the DB with the tables back. No data at stake (greenfield).
- **NEXT: the cutover milestone (merge to main) — the ONE hard stop, human-gated. Remaining before handback: final gap-review + CHANGELOG.**

### Final merge-readiness gap-review (2026-09-15, coordinator — after c4.9f)
Isolation: COMPLETE. Ripgrep-verified herobids apps/**/src AND packages/**/src (ex-tests) have ZERO live access to the 16 dropped tables (no direct query, no import of dropped table objects, no removed repo class) — only type-only mentions, comments, BotRepository.isConnectionOwnedBy (reads KEEP connections), and the KEEP tables remain. @herobids/db build + apps/api + apps/worker tsc clean; db suite 138/0. The c4.9 trading-table EXTRACTION is engineering-complete.
BUILD/migration: sound. 0070 is a hand-written custom DROP (drizzle-kit's rename/delete resolver can't run headless); IF EXISTS + CASCADE is correct for the reset-and-run volume-wipe+replay greenfield flow; _journal.json + 0070_snapshot.json updated.
⚠ **CUTOVER GATE NOT YET SATISFIED — the migration is NOT merge-ready.** The ledger's own gate (001 line ~248: "a Deferred (required for cutover) entry does NOT get to explain-and-pass — it must be resolved or re-accepted as a signed-off Gap") has UNRESOLVED Deferred-required entries beyond the c4.9 table set — these are pre-existing L3 obligations, NOT introduced by c4.9, but they gate the merge:
  1. **checkVenueAccountLimit** (001 ~254, MEDIUM) — per-plan venue-account cap silently stopped enforcing (local venue_accounts ~empty post-boundary). Direction decided (option C: boundary count_venue_accounts read tool + herobids enforces) — NOT implemented.
  2. **adjust_risk_limits** (001 ~307) — Deferred-required; mutates the risk contract over the boundary.
  3. **pull-based usage metering** (001 ~313) — Deferred-required.
  4. **price/market-data staleness swap/dex carve-out** (001 ~386) — Deferred-required.
  5. **watch/price/market-data tool subject-scoping** (001 ~457) — Deferred-required.
  6. **economic-calendar acquisition** (001 ~493-500) — human-ruled Deferred-required.
  + the gate row (001 ~235 "All Deferred (required for cutover) entries resolved") = PENDING.
Plus the ACCEPTED Gaps needing explicit sign-off at cutover: g1 decision-failure persistence, g2 llm-artifact persistence (neither side persists until the agent-reasoning-runtime relocates — greenfield, no history lost).
Also inherently HUMAN/operational (cannot be code-satisfied): merge-gate items (3) run locally + on staging a while (manual/visual/black-box) and (4) human approval; and the operator-config touchpoints (allowedActorTypes:['system'] for the alert-dispatcher consumer; SCRAPFLY/LLM env for the boundary).
BOTTOM LINE: c4.9 (trading-table extraction + drop) is DONE and isolation holds. The MIGRATION is not merge-ready until the remaining Deferred-required entries (1–6) are resolved-or-signed-off and the human runs the staging/approval gate. These six are the next work; each is its own slice under the same loop.
